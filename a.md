# 组播（Multicast MCAST）项目简历描述（详细版）

> 来源：`项目SOW/` 下 31 份组播工作任务书，每份由独立 subagent 通读全文分析并逐条扩写（每条 800~1200 字量级）。代码量为任务书官方 LOC 数据（标注"折算"者为任务书给出的折算值），功能描述均取自任务书原文。

## 一、组播大规格与转发平面优化

### 1. 组播按需下发（不同板组播规格叠加）｜ 园区核心交换机 S10500V7 ｜ 4300 行（新开发）
- **背景**：组播硬件资源分复制资源（全局，各板都需要）与前缀资源（本板，仅流量入口板需要）。大规格模式（使能 [ipv6] multicast replication-share enable）下平台虽以"创建/绑定复制资源"命令字下驱动，但各板都下发绑定命令，驱动随之创建前缀资源，各板表项完全一致：以单板定制 100k、4 块接口板各打 25k 流量为例，每板仍占满 100k 前缀资源；且表项 ID 由主控板统一分配、作数组下标并同步各板，各板规格必须一致，整机表项规格被限制为单板规格、无法随板数扩展。目标是非流量入口板不下发绑定复制资源，使整机规格为各板叠加（该场景整机放大至 400k）。
- **功能**：入口板判定有"流量法"（未知组播上送）与"接口法"（按入接口依附板）两种，经三方案评审（接口法对聚合/VLAN/VSI/Tunnel 等全局口难以精确获取成员板、Tunnel 口无法支持；方案三全局口不支持则功能缺失）选定**方案一流量法**：仅流量入口板下发绑定复制资源，流量切换后在新入口板经未知组播上送重新触发。逐协议适配：**PIM SM**——注册报文为单播、可能从任一板收到，接口板建 dummy 表项、转正后在所在板（如 slot2）下绑定复制资源；SPT 切换时源侧单播路由出口可能在其他板（如 slot3），引流后 slot3 未知组播上送触发表项下发，slot2 旧表项等老化删除，RPT→SPT 类似；**PIM SSM**——原流程下游加入即下驱动、流量到达不再上送未知组播，需适配为等流量到来再下驱动绑定复制资源（创建复制资源时机不变）；**PIM DM** 原本流量触发、无影响；**IGMP Proxy** 与 SSM 处理类似；**双向 PIM** 不支持大规格模式、不涉及。另覆盖 PIM FRR（主备入接口所在板均需下驱动，备份入接口流量可触发上送）、MVPN MDT/NG-MVPN（default-group 在主控板 dummy 转正、靠私网 hello 维持；data-group 老化后切回重建）、MVXLAN（入接口随流量选 VSI 虚接口或 MUPE 口，经入口变化检测切换）、跨 VPN（接收者 VPN dummy 表项主控板创建，是否下驱动看源 VPN 表项流量入口标记）。新增系统视图命令 `[ipv6] multicast forwarding-conversational-learning`（undo 关闭），命令维护在 mrib、下内核 mfib 并板间同步；非大规格模式允许配置但不生效并提示；新增按需学习产品定制项（登记在 MRIB 与 PIM 配置插件中）。表项打上流量入口标记（原标记字段已满，放入扩展标记字段）。表项 ID 规格动态化：接口板启动经板间消息上报本板 IPv4/IPv6 规格（新增携带全局 slot 号及堆叠框号/板号），主控按全局 slot 号排序维护双链表并累加，新增表项规格板间消息（携带全局最大表项 ID 与 IPv4/IPv6 最大表项数目）通知各板放大表项 ID 规格；规格只增不减，备升主后各板重发；表项 ID 分配/索引数组扩容先拷贝后 RCU 释放旧内存，主控板规格 = 各板 IPv4+IPv6 之和 +1。10500 XC 按单板 8k、系统总规格 128k 在 IPv4/IPv6 组播产品定制配置中新增定制，无业务口的板上报规格 0。新增 `display [ipv6] multicast forwarding-table capability [ slot slot-number ]`（Local/Global entries、Software entry index、各板 Capability）；display multicast forwarding-table 增加 local-in 关键字并显示扩展标志信息（FlagExts）；display system internal multicast capability 增加全局规格、本板是否支持流量统计；NETCONF/YANG 在组播路由表与 IPv4/IPv6 全局配置表中新增 ForwardingConversationalLearning 列。按需判断加在三层表项绑定复制资源的下驱动入口（并同步控住入接口切换、P2MP 隧道下发两条关联流程），非入口板软件仍绑定复制资源、仅不下驱动；未知组播报文经转发查表信息获取流程走 no-cache，未命中处理流程对已存在表项达一级内存门限也继续下驱动。
- **难点**：全局口成员板不可精确获取，流量入口切换需在新板重新触发下发、丢包时间变长（FRR 可弥补），且主控板不下驱动、无法携带 drv-context 给接口板；SSM/IGMP Proxy 存在打流到下驱动完成的丢包窗口；表项 ID 规格动态化引入两条新增板间消息，需保证板间时序（接口板先按本板规格初始化、备升主后重发），数组周期遍历性能浪费通过在表项结构上新增流量统计与流量上送回调两类挂接节点挂链、按表项 ID 记断点分批遍历优化；模式切换（评审选定方案二：不处理、手工 reset mfib 恢复）受表项 ID 一经分配不可变更、只增不减约束，且 IPv4/IPv6 表项 ID 规格共用、可能单栈使能。
- **代码量**：总 4300 行（18 个功能点全部新开发，C，无移植），整体难度与复杂度较高、周边模块影响度较高、验收测试工作量较大、风险较高；功能点难度系数 0.7～1.5（未知组播流程修改 1.5、表项规格板间消息 1.4 最高），综合难度系数约 1.45，涉及分布式、多线程、跨板数据备份、复杂组网；人力投入 3 人（周凯阳、林方钰、袁廷坤，设计负责人刘海丹），配置插件占比 5%、用户态占比 20%。

### 2. 大规模提升组播路由规格（复制资源复用）｜ Comware V9R1 ｜ 12500 行
- **背景**：组播复制资源规格受硬件限制（园区核心 8k/16k、园区接入 IPv4/IPv6 各 4k、数据中心 16k、SR88 组播专用 MID 规格 32k），与表项资源相互独立且难扩展。原实现出接口必须依附表项下驱动：SG1、SG2 出接口同为 IF1+IF2 时需下发 SG1+IF1+IF2 与 SG2+IF1+IF2 两条，各占一条表项资源+一条复制资源，出接口被重复消耗。复用 group 资源需驱动+平台同时修改（需求号 202202089842）；项目为 V9 现有模块增强、B 类新 V 模型，功能均在内核态（仅 CLI 插件用户态），软转/快转沿用原流程。
- **功能**：将相同出接口提取为独立"组播复制资源"先下驱动，表项以引用计数关联共享，实现"N 条表项+1 条复制资源"。主控板计算复制资源 key、分配全局唯一 index、下驱动返回产品上下文信息，随 PIM→MFIB 板间消息同步给接口板；接口板按主控 index/产品上下文下驱动，出接口列表按本板最新表项自行计算，创建/删除按引用计数判断。新增复制资源创建/删除/追加/移除与表项绑定等驱动命令字（复制资源复用原表项结构，VLANIF/SUBVLAN 等标记随资源下发，表项绑定携带复制资源产品上下文）。逐形态展开：三层路由口（VLAN 虚接口以出接口+出端口求和为 key，入接口索引入校验，VLAN 入口打专用标记）；super-VLAN 三级 key（出接口索引+sub VLAN ID+出端口索引，维护入 sub VLAN，结构在出接口与出端口间加一级 sub VLAN）；VSI 虚接口类比 super-VLAN（VSI 实例+AC 口）；MVPN MD 模式 MTI 口以"MTI 接口索引+data-group 地址+封装源地址"参与 key，default-group 仍随 MTI 下发，data-group 切换走独立下发流程；NG-MVPN RSVP-TE 隧道口 VN handle 不参与 key，VN 变化沿用原有下发流程；MUA 口（索引 0xC0000000-0xFDFFFFFF）与 MUPE 口按接口索引参与 key；跨 VPN 场景源 VPN 表项将跨 VPN 虚口扩展为接收者 VPN 真实出接口（如 IF1+IF2+IF3）合并维护复制资源，失败侧同步"不下驱动"标记仅维护软表；双向 PIM 仅 (*,G) 转发表复用，DF 出口列表仍用原方式下发，RP 表项不共享（规格小、资源独立）；MVXLAN 头端公网表项一次查表不下硬件，按旧方式下驱动；dummy 表项无出接口不占复制资源；二层组播（igmp-snooping/pim-snooping/MVLAN）本次不开发。功能开关：产品定制开关字段三取值（不使能/半使能/全使能，对应配置文件中的复用类型字段），命令行 multicast/ipv6 multicast replication-share enable 在 MRIB 注册并存 DBM，主控板启动读 DBM、接口板经 SYNC 流程在同步表项前同步。显示命令：display [ipv6] multicast outgoing-interface share info（含 replicate-index/出接口过滤，显示 Drv Context、Match key、引用计数）；forwarding-table 及 verbose 增加 Replicate index/match key 字段并新增 replicate 过滤；multicast record 增加 rep-index/rep-drv-context 过滤及 append/bind/create/destroy/remove-l3-replicate 记录项；event-log 补全 OIFSHARE SEND/RECV EXTL2PORTSLOT、INDEX REPEAT 日志。ISSU 约束：复制资源需单独维护数据，非项目版本与默认使能复用的版本间不能升降级；命令行可控版本关闭复用后可与非项目版本互升降。
- **难点**：出接口数量不定无法直接做 AVL key，设计为一级 AVL（出接口索引 64 位算术求和作 key，key 缓存在 MFIB 表项上）+冲突链（记录出接口列表与个数校验）+全局唯一 index 二级树，每 MDC 一份，接口板无 index 树；非稳态主备倒换下主控/接口板复制资源 index、产品上下文、出接口三者不一致（互换会致表项互串），采纳 V7 评审结论：index 取值 1-0xFFFFFFFF 递增分配基本不重复，驱动校验产品上下文与 index 不一致返回失败，平台周期重刷恢复；复制资源不支持 modify（避免记录出端口失败状态），表项切换复制资源失败时记 old index 避免直接删除断流，产品需保证切换复制资源不丢包；二层端口扩展时序——接口板 MCS 消息不再自行扩展三层表项，由主控板维护完二层软表后构造新的二层端口扩展消息（复用既有组播控制消息结构，携带复制资源 index/产品上下文）在原出接口扩展消息之前下发；V9 适配打包批量下驱动（修改驱动批量处理桩）、IPv4/IPv6 两套流程合并为单一源文件、复用配置经公共特性启动框架拉起的 MFIB 线程读取并板间同步；下驱动失败重刷须先刷复制资源再刷表项，跨 VPN 失败侧不重刷不加重刷链。Simware 驱动桩：MDC 下新增复制资源 index 树（index 冲突校验）、产品信息树（上下文冲突校验，释放后优先复用）与组播前缀树（表项依赖校验），异常直接报错抓第一现场，配套 probe 命令 display system internal multicast replication-share driver resource/entry，仅 Simware 定制安装。
- **代码量**：总 12500 行，按 18 个需求功能点分解（复制资源基础数据维护/下驱动基础流程/Simware 驱动桩/命令行前后台等 9 项移植，打包下驱动适配、IPv4/IPv6 代码复用、配置通知与板间同步等 9 项 V9 适配），单项 LOC 200~1000、难度系数 0.9~1.95，整体难度系数 1.5，C 语言；难度/复杂度、验收测试工作量、风险均为高，周边模块影响度较高；人力投入 2 人，SOW 撰写 80 人时；开发/设计负责人马晓忠；验收产品为园区接入交换机，功能验收产品为 Simware 及不含此特性的 V9 产品。

### 3. 组播复制资源复用模式跨板优化及 FRR 完善｜ S10500V7 ｜ 7000 行（新开发）
- **背景**：大规格模式（复制资源复用+按需下发）下，硬件只能按入口板携带的单个复制资源索引查表。入/出接口跨板时，出口板增删出接口触发复制资源新建/删除，与入口板绑定时序错位（先绑新资源而出口板未创建、或先删旧资源而入口板未切换），均致原有出接口丢包/断流。"双资源索引"硬件不支持、"出口板重查前缀表"浪费 ACL/EM，最终采用"复制资源等各板回应"方案。另有 FRR 备份入接口为 VLAN/VSI 且兼作出接口时表项维护不全、主备板信息不一致，及 32K 表项下主 VLAN 口切换偶现断流 20~60 秒（MFIB 串行调整相关表项挤占 FRR 检测）两类遗留问题。
- **功能**：①跨板两阶段确认：新增复制资源创建完成消息（接口板→主控板，独立消息结构，兼顾 ISSU）、表项绑定消息、复制资源删除消息，及"先建不绑"标记位。主控板创建复制资源后不立即绑定，生成创建中板位图，收齐各接口板确认后统一下发绑定；引用计数减为 0 挂待删除链，超时10秒后统一删除并通知各板。复制资源新增"创建中/删除中"中间状态，主控板以 AVL 树（key 为表项 ID）维护依赖表项；覆盖拔插板（清位图提前绑定、先平滑复制资源再平滑表项并按最终表项校验）、主备倒换（备升主重挂链重设超时）、失败重刷等场景。②FRR 备份入接口（VLAN/VSI）兼作出接口修复 8 个修改点：NSR 恢复备份入接口、ASSERT winner 被忽略致 loser 无法恢复、PE 口单独流程打包同步、普通端口经入参区分兼作备份入接口与出接口的扩展下驱动、VLAN 口与二层表项关联解除条件修正、板插入同步备份入接口、大规格/FRR 资源复用适配。③性能优化：VLAN/VSI 二层端口扩展改两阶段异步分批，map 树挂变化链按最终状态抵消中间态，每批 32 个表项出接口，复用 MCS4/MCS6 消息队列；VLAN 口 FRR 切换断流从 20~60 秒降至毫秒级。
- **难点**：硬件无法双索引或二次查表，只能在平台软件引入跨板确认协议；确认机制引入创建中/待删除、表项新旧复制资源并存等中间状态，需处理超时兜底、拔插板、主备倒换、失败重刷、按需下发、精确转发等时序；分批改造覆盖 5 个历史问题单场景且需正确抵消中间状态。
- **代码量**：总 7000 行（新开发，C），30 个功能点，难度系数 1.706，整体难度/周边影响/风险均"高"，人力 4 人；扩充复制资源、转发表项、VLAN 映射三类 display 命令（Temporary 计数、状态变化标志等）。

### 4. B70D064SP 特性同步到 V9 trunk——组播驱动接口适配｜ V9 trunk ｜ 6500 行（新开发6000 移植500，折算6180）
- **背景**：V9 转发进程/驱动进程分离，组播下驱动改为跨进程 TLV 消息；DC、接入产品由产品自实现收发解析，SR88、园区核心上 V9 时间紧，要求平台保持驱动接口与 V7 一致以减少适配。基于 LRPC 框架实现：转发进程侧改为 LRPC 远程调用（指明模块/函数 ID、dlsym 查符号用的函数名、长连接、返回值类型及 T/L/V 形式入参），下驱动频繁用长连接+LRPC 共享内存直接传递报文缓冲区（免拷贝）；解析框架落点四方案比选，评审选定方案二：新增 LRPC 插件动态库承载解析，随模块裁剪，框架读插件配置文件加载。
- **功能**：下驱动消息按 82 种一级 TLV 打包、每 TLV 独立返回值；插件逐条解析还原为 V7 形式 "_Real" 驱动接口，IPv4 共 21 个、IPv6 共 18 个（二三层共用表项操作 1 个，二层专用使能/drop-unknown/路由器端口/组播 VLAN/source-deny/VLAN 转发模式 6 个，三层专用使能、转发/实时计数、MTI 封装与统计、M-LAG/VSRP 规则、复制资源复用、VXLAN 逃生、BIER MVPN SA、防攻击等），结果逐级回填。命令字覆盖三层表项头/出接口/出端口增删与入接口切换、双向 PIM RP/DF 与 BIDIR 使能、VRF/接口三层使能、RPF 失败报文 6 种处理、FRR 主备入接口流量检查、M-LAG 奇偶规则（V9 改单接口批量下发）、复制资源增删绑与复用使能（含配置/生效值）、报文上送（VXLAN 三层按 VNI 上送与 DCI 隧道上送为移植）及查询通知接口。改进返回值机制：原 TLV 返回值初始化为成功、异常时误判已处理，评审选定方案二——总返回值失败=TLV 全部未处理免回填，成功则必须回填所有 TLV 返回值。simware9 新增驱动桩动态库（随机填充驱动上下文、模拟复制资源）及 CLI 插件（debugging 输出 TLV 解析、set 控制指定 TLV 返回值、display 桩状态，复制资源命令/数据迁移至驱动进程），评审确定驱动进程新增 CIOCTL server。
- **难点**：82 类一级 TLV 逐条解析与 V7 结构还原（V7 结构体名被 TLV 占用、加 DRV 标识重定义，驱动上下文拆全局/本板两份）；表项与出接口一体/分离两种失败重刷模型——一体整体成败只填表项头返回值、失败整体重刷；分离表项头成功后逐出接口回填、仅重刷失败出接口且必须填全返回值；单 TLV 失败须继续处理后续 TLV；长连接+共享内存+批量打包降低跨进程开销。
- **代码量**：43 个分配需求功能点；总 6500 行（新开发 6000 移植 500），折算 6180 行；难度系数 1.2375；人力 3 人；整体难度/周边影响/风险较低，验收测试工作量中；先在 simware 验收，DC/接入仅需配合改总返回值逻辑 1 行。

### 5. B70D064SP 特性同步到 V9 trunk——组播大规格优化等｜ V9 trunk ｜ 11700 行（新开发5800 移植5900，折算6900）
- **背景**：B70D064SP 园区核心交换机特性同步到 V9 trunk（需求 202501210075-3，移植类、现有模块增强），含需求 1 大规格跨板/FRR/性能优化与需求 2 B70/B75 MSDP NSR，均为我司特有实现。V9 消息体系与 V7 不同：PIM 下发 MFIB 改为 TLV+CTL，下驱动改为 TLV+IPMC 先打包、经 LRPC 调用驱动进程接口（批量下驱动采用共享内存、转发进程下驱动），相关流程需整体适配。
- **功能**：需求 1.1 复制资源复用跨板优化——跨板创建/删除复制资源时各板时序不一致导致出接口丢包，新增 4 类板间消息（复制资源创建完成、表项绑定、可删除通知、删除执行）及"先建不绑"标记位；新增创建中/删除中中间状态、创建中/待删除板位图与各板确认机制，超时 30s/60s（共用 1 秒周期定时器）；表项以新旧两个索引记录复制资源，主控板在创建中资源上维护以表项 ID 为 key 的 AVL 临时树；评审决策待删除资源不复用；适配主备倒换、拔插接口板、按需下发。需求 1.2 B70 FRR 完善——备份入接口为 VLAN 虚接口且兼作出接口（下驱动需扩展两次），修复二层端口变化（加入/退出聚合口/VLAN、成员口 up/down）导致的表项维护不全与主备板信息不一致；涉及全局口出接口节点创建、NSR 恢复备份入接口、PIM ASSERT loser 恢复、VLAN 映射表项删除等 8 处修改点；大规格复制资源 AVL 树不存重复出接口，改为入接口维护二层端口树；并适配 FRR 资源复用与出接口资源复用、板插入备份入接口同步。需求 1.3 MFIB 性能优化——VLAN 二层端口扩展由同步改为异步分批（先处理 32 个，其余写入转发消息队列，按最终状态在映射变化链上抵消中间时序），消除 FRR 断流 20~60s（问题单 202411011212：32K 组播组下主 VLAN 口切换断流）；二三层映射源结构中指向二层表项的指针改为枚举类型，并扩充二三层映射关系可维护性显示；优化后保持大规格表项下 FRR 切换丢包小于 200ms。需求 2 B70/B75 MSDP NSR——配置数据（MSDP 视图 VPN、cache-sa-enable、encap-data-enable、源策略、keepalive 间隔、debug 开关；peer 地址、mesh-group、MD5、SA request、SA 缓存上限、sa-policy、静态 RPF 等）与运行数据（peer 状态机、TCP 连接状态/源目的端口、SA 表项增删/reset）实时批备，备升主以备份数据为准平滑 DBM，处理 VRF index 校验、三级内存门限、接口/VPN 事件；远端源以 300ms 定时器、每次 1024 个平滑给 PIM；使能 NSR 需同时使能 PIM NSR，发包方式改为先备份发送缓冲区、收到 HA 备份成功 ACK 后再发送。V9 TCP NSR 纯新开发：基于 Linux TCP Repair 机制（套接字修复模式与收发队列/序号选项），备份接收报文队列（每连接 LIPC 单播、按序编号确认、去重、30s 失败去使能 NSR），应用层备份发送缓冲区与接收/应答序号信息，实现 TCP 连接无缝迁移：修复模式 connect 直接置 ESTABLISHED、拦截内核 FIN/RST、新 socket 内核控制块与旧连接关联、10 分钟超时回收端口。
- **难点**：跨板复制资源确认/超时时序，覆盖主备倒换、拔板、新插板等异常场景；FRR 备入接口与出接口双角色驱动扩展；异步分批后中间状态时序抵消（表项震荡、二三层使能切换、fast leave）；V9 不能改 Linux 内核 TCP 栈，须用户态完成序号/缓冲区/选项迁移并处理 TCP 时间戳、inpcb 平滑老化。
- **代码量**：总 11700 行（新开发 5800 移植 5900，折算 6900 行），55 个分配需求功能点，难度系数 1.50722775；整体难度高，周边模块影响度较低，验收测试工作量较大，风险较高，人力投入 3 人。


## 二、EVPN VXLAN M-LAG 组播

### 6. 组播补齐一期（B70 MULTICAST EVPN 一期 组播 VXLAN 基础特性补齐）｜ V9 trunk ｜ 88.2K 行（一期75.8K：移植65.2K 新增10.6K）
- **背景**：V9 TRUNK 主线移植 V7 B70 分支已商用的组播 VXLAN（MVXLAN）基础特性，汇集 7 个来源需求：中国广电核心复制（leaf 按需复制组播流，38.2K）、腾讯手工 VXLAN 模型三层组播一期（2.5K）、北京地铁 11 号线 125G75E 的 EVPN&传统组播 M-LAG 组网（7.7K）、S5560X DRNI 场景 IGMP proxy（14.5K）、125G 设备 EVPN 组播支持 IPv6（9.8K）、NEC 6800 EVPN VXLAN 二层组播 IGMP PROXY（9.8K，仅移植三层核心复制依赖部分）、DRNI 切换 M-LAG 的 MCAST 需求。一期仅保证 CLI 功能，NETCONF/MIB 留二期；V7 跨 VPN 功能、二层双归属下发驱动、IP/MAC 转发模式、ES 事件、IGMPv3/MLDv2 exclude 本期不支持。
- **功能**：① MVXLAN 三层组播核心复制（38.2K）：系统视图 multicast-vpn vxlan vpn-instance instance-name mode { ingress-replication | mdt }（含 public-instance）建 MVXLAN，地址族视图配 data-delay、data-group [ acl ]、default-group、source [ evpn-mlag-group ]，接口视图 pim distributed-dr；BGP 6/10 类路由携带 VPN RT 与 PMSI Tunnel 属性、SBD-IMET/SBD-SMET；驱动经三层组播路由表项变化驱动接口下发头/尾端公私网表项，头端每 data-group 独立 Mtunnel 口携带 L3VNI 与 VSI 索引，尾端出口为 MVXLAN 用户侧（UPE）虚拟口解封装。② 手工 VXLAN 三层组播：MFIB 扩展 Vxlan 端口、软转与硬件下驱动。③ EVPN VXLAN 二层 IGMP PROXY：VSI 视图 igmp-snooping proxy enable，经组播协议事件（代理使能/去使能、端口增删、表项增删、ES 拓扑变化、组播路由更新等）与 L2VPN 交互，支持 6 类路由（7/8 类不支持）。④ M-LAG：m-lag local/remote 虚拟源地址与故障切换、data-group 同步、PIM/MD 响应 DRNI 事件、triggered-query/triggered-hello 快速切换（防火墙）、BGP AD-PATH。⑤ L3VNI VSI 虚接口 IGMP proxy：igmp proxy enable，MD 发布 IMET、维护成员关系，SMET version/include/exclude 下发 MD，驱动支持 (*,G)。⑥ EVPN 组播 IPv6：MD/MRIB6/PIM6/MLD 适配、SSM、Anycast-RP、s-pmsi advertise source-active。⑦ DRNI→M-LAG 双关键字：drni 命令行隐藏注册（脚本下发成功），debug/display 拉平 V7/V9。⑧ COMMIT 基础（MD/MCS/PIM/IGMP）与增强：mode 冲突经命令行配置树查找检查（mdt/rsvp/mldp/evpn-mdt）；default/data-group 重叠冲突需维护 MVPN 数据组按地址排序等 URL 配置数据；source 与 m-lag local 须 ipv4/ipv6 一致。
- **难点**：V9 沿用 V7 框架、仅 MFIB 移入转发进程独立线程，引发通信适配：IGMP/MD 与 MRIB 间 LIPC 消息 V9 需转字节序；MRIB 下发 MFIB 的消息由"节点+控制库+接口库"三个结构拼接改为"头部+子 TLV"结构（13 类枚举、新增 L3VNI TLV）；MCS 下 L2MFIB 表项由 CIOCTL 改 LIPC 下发到转发进程 L2MFIB 处理线程；MFIB 下驱动接口收敛为统一的三层组播路由变化处理接口，按 LIPC 保证对齐与字节序；MCS/MFIB 的 epoll 回调注册重写。COMMIT 增强需维护 URL 配置数据。主模块 MVXLAN/PIM/PIM6/MRIB/MRIB6/MFIB，L2VPN/BGP 全职配合；多线程+分布式+板间备份+复杂组网，难度高、风险高、验收测试工作量大；VPP/软转不支持。
- **代码量**：一期 75.8K 行（移植 65.2K / 新增 10.6K），与二期合并 88.2K；86 项分配需求，难度系数 1.25，用户态占比 50%；6 名开发并行（设计负责人 yangjie、开发负责人 yuantingkun）；特性合入 V9 交换机产品，S5560X 明确联调。

### 7. B70 MULTICAST EVPN 二期（V7V9 特性补齐专项）｜ V9 trunk ｜ 33700 行（移植20700 新开发13000）
- **背景**：属"V7/V9 特性补齐"专项二期（需求编号 202207127890，SOW 拟制袁廷坤），在 V9 TRUNK 园区交换机上以"现有模块增强、移植类"方式补齐 V7 已有而 V9 缺失的组播/EVPN 能力，多数子需求移植自 V7 分支项目（ADDC 广电/NEC/太原轨交、河南移动运营商 trap 补齐二期、NEC JR、深交所等），与一期同系列，NETCONF/MIB 原 V7 项目不涉及、二期一并补齐。特性运行于 CEN/MPU/LPU，纯用户态实现、不涉及内核态与新加进程，分布式同步走内核态，进程级 HA 采用 M3 模型（主备同步+支持重分布）；MLAG/vSys 天然支持，启动阶段免；新增命令行均需支持 commit，并适配 XML2CLI、ISSU、进程亲和性，运维告警以 TRAP 方式提供。驱动侧本次仅支持 simware9 仿真，产品不配合，验收产品为 SIMWARE/AR/S 系列。
- **功能**：①非对称跨 VPN 组播路由互引：MRIB 视图 `multicast extranet select-rpf [ l3-vni vxlan-id | vpn-instance vpn-instance-name ] { group group-address { mask | mask-length } | source source-address { mask | mask-length } } *`（IPv6 对应 ipv6 MRIB 视图同构命令），vpn-instance 字段 mcast 同步 v9 二期已支持，本次只支持 l3-vni 字段（专用于 MVXLAN 组网）；新增 Probe 命令 `display system internal pim l3-vni vni-id routing-table`、`display system internal multicast l3-vni vni-id forwarding-table`；ExtranetSelectRpfs 两张表（IPv4/IPv6）整表增加指定 L3VNI/来源 VPN 列；平台将跨 VPN 表项合并，无需驱动修改；含跨 VPN 策略配置、PIM/MFIB 特殊表项维护、L3VNI 与 VRF 关系维护、对称/非对称 VPN 切换、接收者侧流量转发。②EVPN 三层组播 DCI 互通：MVXLAN IPv4 地址族视图 `dci enable`（不支持 IPv6 地址族故 IPv6 不支持 DCI），MVPN/Instances 表增加 DCI 使能列；BGP 10 类路由发布组播源、MD 与 BGP 交互、DCI Tunnel 建立及 MCS/MD 维护 VXLAN DCI Tunnel 出口、源/点播者侧跨 DC 组播流量处理、L3VNI 映射、组播源迁移与重生成路由交互维护。③3ED 组 DCI 三层组播：系统视图 `multicast-vpn vxlan edge remote remote-ipv4-address`，多 ED 场景转发设备选举与故障切换。④M-LAG DCI：系统视图 `multicast-vpn vxlan m-lag local local-ipv4-address remote remote-ipv4-address`、`multicast-vpn vxlan dci switch-delay value`，MVXLAN IPv4 地址族视图 `dci switch-delay value`（IPv6 不支持），MVPN/Instances 表增加 DCI 切换延迟列（列属性仅在特定 mode 下支持，全局配置不支持 NETCONF）；支持分布式聚合 ED、故障恢复延迟切换。⑤EVPN 二层组播 IGMP PROXY 及 ES 组播（部分功能已支持，本次主要实现 ES 对组播的支持）：VLAN/VSI 视图 `igmp-snooping/mld-snooping forwarding-mode { ip | mac }`，新增/增强 `display igmp-snooping/mld-snooping evpn-group`、`display igmp-snooping group`、`display l2-multicast ip`（及 IPv6 版）等显示（支持分布式 slot/chassis）；非 EVPN 路由发布模式 VSI/VXLAN 代理支持、IGMPv3/MLDv2 exclude 模式、二层组播维护 EVPN 路由表、ES 事件及数据维护（L2VPN 经组播协议事件枚举交互：平滑开始/结束、代理使能/去使能、端口增删对应 6/7/8 类路由、表项增删对应 6 类路由、ES 拓扑变化——ES 邻居 Down、变成 DF/BDF、离开 ES 时发查询、组播路由更新），组播不直接与 BGP 交互而是经 BGPVC 与 L2VPN 打交道，本期不支持 7/8 类路由；双归属 non-DF 口平台仍下发表项但禁止转发（产品天然支持 non-DF 口 ACL 禁转），转发模式经 VLAN/VSI 二层转发模式命令字下发驱动。⑥igmp-snooping access-policy：MCS 前后台代码已有，命令行注册依赖 muserlib，需放开被注释的命令行注册代码并适配修改。⑦组播 trap：PIM 模块部分 trap 节点补齐（运营商 trap 补齐二期）。⑧组播 FRR（深交所）：PIM 视图 `rpf-frr enable [ policy { ipv4-acl-number | name ipv4-acl-name } ]`，系统视图 `multicast frr monitor cycle monitor-time`、`multicast frr wtr { wtr-time | disable }`、`multicast frr mode [ by-entry | by-iif ]`；display pim routing-table（新增 proxy/extranet 显示）、display multicast forwarding-table/routing-table/rpf-info 增加备份链路显示；PIM 备份下一跳/备份入接口维护并下内核，MFIB 处理 ADJ/BFD 事件、VN 与转发表关系数据结构、备份入接口下驱动。
- **难点**：对称与非对称 VPN 切换（策略变化时表项在合并/独立两种形态间切换）；跨 VPN 组播在平台层做表项合并以避免驱动修改；EVPN DCI 中 L3VNI 映射与组播源迁移流程；3ED 多归属 ED 选举与故障切换、M-LAG 故障恢复延迟切换（dci switch-delay）；ES 组播需处理 ES 拓扑变化事件触发查询、DF/BDF 切换、non-DF 口 ACL 禁转及 IGMPv3/MLDv2 exclude 模式；组播 FRR 涉及分布式新架构框架、新切换通道框架、FRR 切换消息收发、按接口/多周期检测、流量检测表项范围优化，新增独立 mofrr 线程检测流量，并对不支持检测备份入接口的产品做适配（软件模拟备份口检测）。难度系数评估表中多线程、分布式、数据备份、复杂组网均为 YES，配置插件占比 10%，用户态代码占比 40%。
- **代码量**：总 33700 行（移植 20700 行、新开发 13000 行），难度系数 1.377，整体难度与复杂度高、周边模块影响度中、验收测试工作量大、风险较大，人力投入 2 人（开发负责人付惠泉，开发林方钰/关毅，设计负责人袁廷坤），最大单项为组播 FRR（备份入接口维护 1500、MFIB ADJ/BFD 事件、mofrr 线程及产品适配各 1200、分布式新架构框架 1500 等），其次为 IGMPv3/MLDv2 exclude 模式（2500）与对称/非对称 VPN 切换（2000）。

### 8. VXLAN 三层组播核心复制支持 M-LAG（公网 ECMP）｜ 园区核心（5560X/6520X 等）｜ 1200 行（功能点合计）
- **背景**：源于济南城轨云与浪潮组播需求合并开发，此前仅最简实现 VXLAN 核心复制，本项目补齐公网 M-LAG DR 双活网关与 ECMP 模式（underlay M-LAG 方式不实现）。核心复制针对 vswitch（VTEP）的发现报文，静态 RP 即 M-LAG 虚地址。
- **功能**：尾端按 system number 奇偶规则加入/剪枝隧道：sys1 发奇数组加入、偶数组剪枝，sys2 反之；响应 DRNI system number 变化事件与 IPP 口 down/up 事件（down 时 sys1/sys2 互换奇偶组加入，up 反向剪枝）；peer-link down 向奇偶组均发加入，恢复后仅发本端奇偶组。表项出接口不扩展 peer-link 口防多包，仅一侧发加入但解封装表项两侧均下发。依赖主线逃生 VLAN 命令 `vxlan multicast-group member enable`（CFS 同步定制）及 VXLAN 组播组成员变化、组播路由表项变化两类驱动接口，处理 VLAN 配置/表项时序及逃生 VLAN 口事件。故障场景：私网 DR 故障无需处理；头端公网 DR 故障流量改从对端出；仅 peer-link 故障竞选失败侧 MAD Down 且奇偶规则放开；脑裂分 MAD DOWN/stand alone；尾端故障时解除奇偶规则、两台各自加入全部隧道。NETCONF 为 Mroute 模型 VXLAN 组播组成员（VxlanMGrpMem）节点（XML2CLI/Candidate）。
- **难点**：①实地址隧道 ECMP 多包：PIM ASM 下实地址隧道（S,G）继承（*,G）全出口，下游收到自身报文且无法检查丢弃；虚地址靠尾端（S,G）RPT prune 剪除出口，故仅支持 M-LAG 虚 IP 方案。②双活网关问题（RFC 7761 二级地址覆盖）：虚 IP 作子地址致下游邻居二级地址震荡；BSM 源地址为主地址而路由下一跳为虚 IP，RPF 失败致 RP 学习失败。评审采纳以虚 IP 作 PIM 邻居主地址（双活网关本期暂不支持）。③peer-link 隔离致头端部分流量无法出隧道，只能配逃生 VLAN 三层逃生；脑裂 stand alone 评审采纳配置优先级方案，虚地址切实地址暂缓；ECMP 尾端逃生必须走 peer-link。
- **代码量**：7 个功能点（尾端奇偶加入/剪枝、peer-link 故障恢复加入剪枝、响应 system number 变化、表项不扩展 peer-link 及软转适配）合计约 1200 行（C，全部新增）；难度系数 1.27，用户态占比 70%，配置插件占比 0%；整体难度评级"高"。

### 9. 浪潮组播 VXLAN 核心复制支持 M-LAG｜ B70D064SP34XX（5560X/6520X）｜ 1900 行（功能点明细合计）
- **背景**：需求 202404090039（合并 202403130079），L公司（浪潮）组网要求核心复制支持 M-LAG，对标 H 友商 16800 的 VXLAN 组播特性（友商无核心复制+M-LAG 组网说明，公网均用 ECMP）。新 V 模型 S 类、紧急开发类。典型组网：裸金属服务器与计算节点 vswitch 接入，vswitch 为 VTEP（隧道源 Data 口 100.200.19.41），vswitch 与核心经 VLAN 3016（类 BUD 节点对指定 DIP 不解封装、广播到核心），核心与接入经 VLAN 10；核心复制针对 vswitch 发出的组播目的 VTEP 发现报文，无三层组播业务流量需求。全网静态 RP，即核心交换机上的 M-LAG 虚地址。明确规格：仅支持 M-LAG 虚地址方案（两端隧道源相同），不支持实地址；仅支持公网 M-LAG DR 接入，underlay 为 ECMP 的方式本期分支暂不开发（主线需考虑）。
- **功能**：三层组播 VXLAN+M-LAG 新增 M-LAG 虚地址核心复制方式（此前仅 ECMP），PIM ASM/静态 RP 以 M-LAG 虚地址为 RP（不支持 SSM）。新增 CLI `vxlan multicast-group member enable`/`undo`（VLAN 虚接口视图，级别 2 配置级，network-admin/mdc-admin；全局仅一个 VLAN 虚接口可配，建议不部署其他业务；重复配置报错 "The vlan-interface of vxlan multicast-group member already exists"；限制：不能多个 VLAN 配置、不能在 VPN 的 VLAN 口下配置），支持 commit/dis this all、CFS 双机配置同步、Candidate、XML2CLI，NETCONF 本分支暂不支持（YANG Mroute/Ipv4Interfaces 表预留 VXLAN 组播组成员元素），不涉及 vSys/MIB/WEB。运行于 CEN/MPU/LPU，涉及内核态，分布式同步由内核态线程实现，Phase3 启动，无新进程/线程，M3 进程级 HA，无 root/CAP 特权。命令下内核和驱动（驱动接口携带 VLAN 接口索引与 VLAN ID 的成员配置结构）；VLAN 接口激活/删除恢复配置、up/down 下发/删除出接口；逃生 VLAN 分布式同步与 HA；配置变化联动表项（先配置后表项则遍历接口链表加出口，反之遍历表项加出口）；逃生 VLAN 变化遍历重刷下一跳表项；下一跳表项添加逃生 VLAN 出口与 IPL 出端口（出接口打 0x10000 Flag 标识 VXLAN 组播成员口）；与 PIM SM 使能联动——均使能才下驱动（构造特殊 JP，display pim routing-table 可见 Flag 含 VXLAN、逃生 VLAN 出口）；ISSU 软重启恢复该配置。主模块 MRIB/MFIB；交付 NETCONF/RESTful/gRPC API 参考、YANG 文件、驱动 API 文档、系统测试用例、命令参考、配置指导、特性说明书等。
- **难点**：实地址方案有环路（两端都发 (*,G) 加入，注册口形成 (*,G)，流量复制到对端 Leaf 且 IPL 也复制导致多包），故仅虚地址（RPF 原则保证头端只发一份）。三大转发原则：peer-link 与隧道侧/DR 侧隔离（peer-link 来的 BUM 只发单挂口/对端故障 DR 口；隧道侧收到的 BUM 不经 peer-link 广播）；核心隧道头节点加封装出 DR 口奇偶分担（国产化芯片环回走三层组播：奇数组从 system-number 1 出、偶数组从 system-number 2 出）；中间节点普通三层组播奇偶分担。由此产生偶数组被 hash 到左侧（或奇数组到右侧）不通的问题：放开"peer-link 流量不往组播隧道出"ACL 的方案不可行（产品无法识别 BUM），只能 peer-link 三层逃生——专用 VLAN 虚接口承载带 VXLAN 封装的逃生组播报文，对端不终结、匹配核心复制隧道下一跳表项往隧道侧转发。故障逐场景：头端私网 DR 故障无需处理；头端公网 DR 故障奇偶限制放开、均带 VXLAN 封装逃生到对端 DR 出；peer-link 故障 MAD 变单机；尾端私网 DR 故障放开 IPL 到对端 DR 的限制；尾端公网故障经单播路由逃生、由对端加入组播隧道（对端本地解封装或 BUD 带封装送回）；ECMP 下对端整机故障需解除奇偶加入规则、同时加入奇偶隧道。性能：私网 DR 故障下一跳表项无变化，公网 DR 故障仅奇偶规则变化，peer-link 故障删除/恢复添加出接口。
- **代码量**：9 个功能点（命令行 400/commit 100/下发内核和驱动 300/VLAN 事件 100/分布式同步和 HA 100/配置变化联动表项 100/遍历重刷下一跳表项 400/下一跳表项添加逃生 VLAN 出口和 IPL 出端口 300/与 PIM SM 联动 100）合计 1900 行（C，全部新增，非移植），难度系数 1.2203296875（整体难度中），用户态占比 50%，配置插件占比 10%，涉及多线程/分布式/数据备份、大量外部模块/驱动配合、复杂组网；验收产品为园区接入国产化芯片交换机 5560X/6520X（含 SIMWARE），SOW 撰写 40 人时，开发 zhoukaiyang/linfangyu，设计负责人 yangjie。

### 10. EVPN M-LAG 拷机组播 mdd/drni 进程互等优化｜ B70D064SP38XX 分支 ｜ 2.3K 行
- **背景**：Comware V7（B70D064SP38XX 分支）问题单需求（需求编号 202505160019），S 类、现有模块增强，属 MD（组播 VPN）模块框架级内部优化。组播支持 MDT 模式 MVPN 与 MVXLAN，均会创建 MTunnel 口：default-group 的 MTunnel 随 mvpn/mvxlan 视图生成（每 VPN 实例仅一个）；data-group 的 MTunnel 仅 MVXLAN 配置 data-group 后随流量切换生成，跟随流量震荡反复创建/删除，设备可创建总数受产品定制限制（由 MFIB 记录总数与已申请个数，交换机规格 1024~4096 个）。mdd 进程仅主线程与 IFM 线程两个线程：MTunnel 创建/删除由主线程同步等待接口管理完成，ACL、DRNI、L2VPN 等需组播回复的事件也在主线程同步响应并回复。EVPN M-LAG 组网带流量拷机时 MTunnel 大规格震荡，形成 md→Ifm→acl→md、md→Ifm→drni→md、md→Ifm→segrt→bgp→md 互锁环，设备打印 mdd 的 LIPC 超时、mdd 与 drni 进程互等。
- **功能**：SOW 评审了三个方案——创建/删除移 IFM 线程同步处理（备生主时主线程强制等 L3VPN/接口管理平滑结束，进程起不来）、主线程改全异步、需回复的外部事件移 IFM 线程（不利扩展），评审结论采纳方案二。新增主线程通知 IFM 线程的消息框架：主线程调用异步接口创建/删除 MTunnel，每次调用产生的 fd 加入主线程 epoll；接口/地址/L3VPN 事件响应移回主线程，IFM 线程仅接收接口管理、L3VPN、地址管理消息并通知主线程处理。创建流程：先向 MFIB 申请接口编号，满规格时 default-group 返回资源不足、命令行提示超规格配置失败，data-group 表项一直走 default-group 转发并挂入切换失败链，待其他 MTunnel 释放后再尝试切换；先按接口编号+模式在待删除链上找可复用节点（未通知接口管理删除的），再查接口管理（模式不符返回失败等重刷），否则加入待创建链，仅链上第一个节点发起接口管理调用，MVXLAN data-group 场景一次最多批量创建 64 个，删除则串行逐个进行。链节点自带状态机（创建/删除 + 已通知标记）：待删未通知可直接复用、已通知删除则等删除回复后再通知创建、已创建未通知可直接删节点、已通知创建则等创建回复后再删。创建成功按 default-group/data-group 分别处理：通知 L3VPN 绑定 VPN、地址管理加地址、置 link up/net up、通知 PIM MTunnel create/up 并建下一跳表项、发送公网加入（SSM 发 (S,G)、非 SSM 发 (*,G)），并更新 MVXLAN 的 IP 隧道与选择性隧道（S-P 隧道）信息；私网 IPv6 创建的 MTunnel 也需向 IPv4 PIM 通知一次。创建失败打失败标记起定时器周期重刷（default-group 借用全局定时器，data-group 用独立定时器，覆盖创建接口/加地址/绑 VPN 三类失败）。data-group 表项状态机（初始→I-P-Tunnel→等待 SPT 隧道→Delay→S-P-Tunnel）：收到 6 类路由而 default-group MTunnel 未建好时保持初始态，等创建成功消息后统一遍历修改 IP 隧道完成切换；切 data-group 前进入等待 SPT 隧道态并起定时器周期检查 MTunnel 有效性，有效经 Delay 切 S-P-Tunnel，无效继续等待且流量始终走 default-group 不中断。主备倒换：default-group 从配置 DBM 恢复接口编号并与接口管理对账、查不到则重建，data-group 从运行 DBM 恢复接口索引；平滑时从接口管理取全部 MTunnel 与 DBM 恢复结果比较，多余的通知删除，并处理"已通知接口管理但未收到回复即倒换"等两类窗口。另以隧道标识为 key 新增两棵 AVL 树维护 MTunnel 全量状态，新增 Probe 显示命令 display system internal multicast-vpn [vpn-instance x | public-instance] mtunnel [...] info（输出 creating/deleting/failed 计数、状态与失败标记等），补充 event-log ha（区分 v4/v6）与 event-log route（通知/收到接口管理创建删除及回复）。
- **难点**：本质是解耦进程间同步互锁环：在设备 fd 总数受限的约束下，用批量创建（一次 ≤64）+ 串行删除 + 待删除链复用，兼顾创建效率与 fd 占用；异步化后 VPN 配置中记录的 MTunnel 接口索引可能为无效值，所有依赖点需判无效暂缓、待创建成功消息后主动补处理；还需覆盖 MTunnel 超规格、IPP down/up（DRNI 事件删建全部 data-group MTunnel）、tunnel-source 变化、SSM/data-group ACL 变化等震荡场景及主备倒换/整框重启平滑，验收测试工作量大（满规格/超规格/反复震荡拷机）。
- **代码量**：功能点明细（主线程通知 IFM 消息框架 200、IFM 线程处理与通知 200、主线程收到通知处理 200、接口/地址/vpn 事件移主线程 100、HA 倒换平滑 100、event-log 补充 100、两棵 AVL 树 500、显示命令注册解析 200、前台显示获取 200、后台处理 500）合计 2.3K 行，C 语言、用户态占比 100%、非移植；难度系数 1.18942788461538（约 1.19）；整体难度与复杂度中、周边模块影响度小、人力投入 2 人、风险中、验收测试工作量大；新增内存开销：AVL 树节点 72 字节/个（约 72K~288K）+ 全局链节点 36 字节/个（约 36K~144K）。

### 11. 天翼云客户专线交换机 VXLAN 静态组播转发（NETCONF 下发）｜ S6800/S6900F/5940 ｜ 1300 行
- **背景**：金融/证券组播业务上云，B70D064SP 紧急开发，9 月联调、月底上线。组播源在云外经 PIM 与专线交换机（VTEP）打通，接入接口可为路由口/子接口/VLAN 口并绑定 VPN 隔离租户；接收者在云内宿主机发 IGMPv2/v3 report，MCGW 截获后经云内控制器通知专线控制器。VGW 集群经 BGP 向 leaf 发布相同公共 VTEP IP，多租户共用 VGW/MCGW。交换机与 VGW 公共 VTEP IP 建立静态 VXLAN 单播隧道，组播封装 VNI 入云；VNI 由云内控制器分配、通知专线控制器下发对应 VSI。本期仅做同 VPN 场景并预留跨 VPN 扩展。IGMP 报文不经隧道、不走 BGP EVPN 路由，控制器在源侧下发静态 IGMP 组；SSM policy 已支持、IPv6 暂不涉及。命令行已可配置，需第三方控制器经 NETCONF 动态下发静态 IGMP 组。
- **功能**：① MVPN/Instances/Instance 表 PmsiTunnelType 枚举新增值 5—Ingress-replication：仅 NetworkType 为 EVPN 时允许；不支持 public-instance；仅支持 address-family，除 VRF/NetworkType/PmsiTunnelType/PrivateAF 外不可配置；PrivateAF 不支持 IPv6。② 新增 IGMP/StaticGroups 表（IfName/GroupAddr/SourceAddr 三索引：IfName 1~47 字符、仅接口名，GroupAddr 224.0.0.0~239.255.255.255，SourceAddr 可选），支持 get/set/create/merge/replace/remove/delete/get-bulk。set 复用 `igmp static-group` 命令行流程：非组播地址下发失败；源地址全 0 不 buildrun；undo all 不支持；VSI 虚接口无子接口，dot1q/second-dot1q 不支持；仅支持 vsi 口的设备允许下发。
- **难点**：整体难度/周边影响度/风险均为低，1 人投入；实现分析采用 TMS 评审、编码前完成。测试重点为两表全操作及 NETCONF 异常/边界值下发；无产品差异，合入所有产品、不裁剪。交付件：SOW、XML API 文档、YANG 文件、需求说明书、测试用例、规格列表、测试报告、静态检查报告、遗留问题与关闭报告。
- **代码量**：约 1300 行，C 语言，非移植：Mvpn/instance 表入方向复制约 100 行 + IGMP/staticgroup XML 插件约 700 行 + 后台约 500 行；难度系数 0.6；多线程/分布式/数据备份为 YES，无开源/非 C 开发/互通/复杂组网；配置插件占比 30%、用户态普通难度代码 70%；用户态占比 100%。

### 12. 19K-X 适配 Comware V9——EVPN P2MP 组播｜ CR19K-X/SR88 ｜ 3000 行（新开发1000 移植2000，折算1610）
- **背景**：EVPN VPLS 网络缺省采用头端复制（Ingress Replication）泛洪 BUM 流量：PE1 须按叶子节点数将组播报文逐份复制发给 RR，接收者增多时 PE-RR 链路流量随叶子数线性膨胀、大量占用带宽。本特性使组播业务迭代 mLDP P2MP 隧道做核心复制：源侧 PE 发布 IMET 3 类包含性组播以太网标签路由（携带 Route Target + PMSI Tunnel 属性通告根节点地址/Opaque Value），叶子节点按根节点路由向上游逐跳发 Label mapping 建立相容性隧道（I-PTunnel）；接收侧 PE 收 IGMP report 后经 bgpvc 发 SMET 6 类路由，源侧 PE 校验 (S,G) 在允许切换范围内后发 S-PMSI A-D 10 类路由触发选择性隧道（S-PTunnel）按需切换，仅存在下游接收者的叶子加入。相容性隧道一 VSI 一条承载全部泛洪流量，选择性隧道一 VSI 可多条承载特定 (S,G)。方案对标 H 友商（V7 参考友商实现、V9 继承 V7），源自 B75 分支 NV202202100026 项目（原项目新增 3.211k/移植 10.607k）。项目为移植类、新 V 模型 S 类，运行于 CEN/MPU/LPU，涉及内核态（隧道重刷），新特性仅支持 simware（暂不支持软转，后续软转 L2VFIB 约需 2k 工作量）。
- **功能**：① mcs↔bgpvc 消息交互：mcs 通知 bgpvc 十类消息——端口添加（向组播转发表加出端口或收到 leave 报文时，需 VSI 使能代理、配置动态 include/exclude、存在 AC 口、且虚接口未使能三层组播）、端口删除、表项删除、代理使能/去使能、组播路由更新（配置 last query interval）、转发模式（IP/MAC 转发模式，避免多 IP 映射同 MAC 时隧道出口不一致导致 MAC 转发产品双份流）、请求、平滑开始/平滑结束；其中 8 类路由（LS）特殊——mcs 处理 leave 时用端口添加消息触发 bgpvc 向 ES 对端同步 leave 并携带最大响应时间（端口老化时间），对端据此设老化并发特定组查询。bgpvc 反向通知 mcs 同样十类（6/7/8 类路由添加触发端口添加、6/7 类撤销触发端口删除、3 类路由添加/撤销触发代理使能/去使能等）。② MFIB 新增 P2MP 依赖表项两级树：上层以隧道 link ID 为 key 的 P2MP 依赖节点（挂在二层组播用户数据下），节点内含 IP 表项源组二级树与 MAC 表项 AVL 树；二层 IP/MAC 表项下驱动前后分别调用批量添加/更新接口维护依赖关系。③ P2MP 隧道-二层虚接口变化数据结构：以 VSI 为 key 的隧道变化节点，其下挂以 link ID 为 key 的端口变化树（节点含仅重刷失败标记、从 MAC 表开始标记、遍历起始源/组地址、组播 MAC，每节点 68 字节）。因驱动以驱动上下文中的 mid 作 key 查表项而 L2VPN 无此信息，隧道变化只能由 L2VPN 经注册的 VFIB 事件通知组播：事件回调创建变化节点、以端口变化消息写入 MFIB 与 MCS 间消息队列；MFIB 线程在二层控制消息处理中转入 P2MP 变化处理流程，按 vsi+linkid 找到依赖表项树并遍历，以 IP/MAC 表项重刷驱动接口、用新的"更新二层出端口"命令类型重下驱动。④ 驱动接口：IPMC 驱动类型枚举新增"更新二层 IP 端口""更新二层 MAC 端口"两类（TLV 内容同原二层出端口添删），主机端口命令经这两类命令携带原表项驱动上下文。⑤ 显示命令增强：display igmp-snooping evpn-group、display l2-multicast ip(/forwarding) 主机端口增加 P2MP (VSI index/Link ID) 接口类型名（含 (E, SUC) 标记）；display system internal multicast record 增加 reflush-l2-ip-port/reflush-l2-mac-port 过滤参数。
- **难点**：① 隧道变化重刷性能与原子性：大量表项依赖同一 P2MP 隧道时需分批遍历重刷，下驱动成功后记录 vsi+linkid 新结构并与刷新处理加锁做原子保护；刷新失败记入待处理链并启动定时器重刷，一批未处理完也入待处理链，删除端口时从待处理链摘除；I/S-PTunnel 不会成为路由器端口，路由器端口无需处理。② 并发保护：转发进程中 L2MFIB 与 MFIB 线程均下驱动，MFIB 线程新增二层组播下驱动流程，并在二层组播数据结构下新增 P2MP 表项互斥信号量，避免 MFIB/L2MFIB 线程互相阻塞。③ V9 差异适配：B75 中 MCS 经 CIOCTL 直接下驱动，V9 改为 LIPC 与转发进程 MCS 线程通信后经 LRPC 远程调用平台驱动接口（TLV 转驱动消息，simware 需平台实现驱动接口桩打印调试）；下驱动框架由 V7 的内核消息/二层 MAC 表项转驱动接口，改为 MFIB 统一的 IP/MAC 驱动处理接口封装、以 P2MP 二层 IP 表项信息结构为入参；另含锁/内存/epoll/LIPC/SYNC/定时器/MDC/内存门限等 V9 公共修改；分支间因 B75 无 MCS EVPN 模块（从 B70 移植）而 V9 已有，需甄别差异不漏必要代码。④ IPv4/IPv6 双栈需适配、ISSU 需适配（升级兼容）、MLAG 天然支持；HA 备份走 LIPC/SYNC/MESH 通道（V9 不允许 DBM 备运行数据）。
- **代码量**：总 3000 行（新开发 1000 / 移植 2000，折算 1610 行），难度系数 1.446（多线程/分布式/数据备份/复杂组网，用户态占比 20%）；整体难度与复杂度较高，周边模块影响度较低，人力投入 1，验收测试工作量中，风险中；验收产品 simware；开发负责人林方钰，设计负责人刘海丹，依赖 L2VPN 主项目（负责人李晓浩，新增 13k/移植 13k）。


## 三、组播协议特性开发（PIM IGMP MSDP 安全）

### 13. 深交所 PIM Source-Proxy（`pim source-proxy enable`）｜ SR88/B75 ｜ 2200 行（新开发200 移植2000）
- **背景**：深交所组播源经腾讯云跨三层接入路由器，腾讯云不支持组播也无法改成二层透传，导致设备收到的组播源（如 20.0.0.2）与收流接口地址（如 10.0.0.1）不在同一网段，PIM 不认为自己是直连源/直连源 DR，不向 RP 发注册报文、流量不通（DM/SSM 下则表现为不触发状态刷新、持续向上游发 JP）。对标华为：NE40E V800R022C10 接口视图 `pim source-proxy enable` 及 VPN 视图 `source-proxy enable`（后者配套 rpf disable/register-source，用于 P2MP TE 隧道出节点场景）；实测 NE8000（V800R021C00SPC100）接口下无该命令，仅 pim 视图下有 source-proxy 且未测出源代理效果。本项目仅实现接口视图下的源代理功能（VPN 视图不实现），运行于 CEN/MPU 用户态，无内核态/新进程，M3 进程级 HA 模型，NSR 涉及（备份 PIM 标记位），MLAG/vSys/ISSU 不涉及（B75 不支持 M-LAG）。
- **功能**：接口视图新增 `pim source-proxy enable [ policy { ipv4-acl-number | acl-name ipv4-acl-name } ]`（undo 不带 policy，只能配 1 条后配覆盖前配；支持 commit），IPv6 同款 `ipv6 pim source-proxy enable ...`；IPv4 ACL 编号 2000~3999（基本/高级）、IPv6 为 3000~3999，ACL 名 1~63 字符不区分大小写，ACL 不存在时允许配置但打印提示，permit 使能/deny 或未匹配则不使能；缺省用户角色 network-admin/mdc-admin。ACL 联动：前台与后台分别注册 ACL 组删除检查事件（以接口索引及 ACL 为 key 保存检查节点），正向注册、反向注销维护 ACL 引用计数，ACL 变更时回调校验并返回成功/失败。SM 模式：评审确定"配置源代理的接口作为直连源"（而非直连源 DR，避免双归双注册），符合策略的表项复用 SPT+LOC 标记并新增 SPROXY 标记，满足 LOC+收流口是 DR+SPT/ACT+RP 可达+非本机 RP 五条件后走直连源 DR 流程（创建表项、打标、触发注册、通知 MSDP）。无路由场景以流量入口作表项入接口：上游信息返回源代理接口地址、路由信息初始化为缺省值；本地源判断增加"表项创建前取流量入口/创建后取表项入接口"及"是否考虑源代理"两类入参，区分众多调用方的不同语义。DM：表项打 LOCAL 标记、触发状态刷新、停止发 JP，补齐入接口更新；SSM：无 RP/注册/SPT 机制，LOC 标记仅影响 JP 发送，适配 mfib 放开 no-cache 对 pim ssm 的限制并上送（表项打 active 标记、上报 active→inactive）。故障/时序：路由丢失与恢复均由 EH 线程 15 秒周期定时器检测新旧路由差异切换入接口，使能源代理后路由未变化的场景处理"非 local 变 local"（SM/SSM 需新增，DM 已支持）；配置变化场景依赖 mfib 每 10 秒上报 no-cache；流量入口变化时判断旧入口路由为缺省值且新入口配置源代理则切换入接口；NSR 场景备进程保存原入接口并传入上游信息生成流程适配；undo 后删除重建 (S,G) 表项（评审方案三）。display pim routing-table 的 Flag 新增 SPROXY 且 flags 过滤列表同步增加；NETCONF PIM/Ipv4Interfaces、PIM/Ipv6Interfaces 新增源代理开关（enable/disable）与策略（ACL 编号/名称）两列，指定策略必须同时指定源代理开关否则报错。
- **难点**：到组播源无路由场景实现复杂——上游信息生成需返回携带缺省路由信息（以路由协议类型取最大枚举值判缺省）的上游结构，且不对该路由向上游认领（跳过路由 claim，避免感知路由丢失），否则只能靠 RPF 失败的入接口切换兜底、产生多余流程；本地源判断函数众多调用方语义不一（部分希望返回 TRUE、部分希望 FALSE），通过双入参区分。方案评审四项：①无路由是否支持源代理——开发建议与评审结论均为方案二（取流量入口作入接口，通用性好、实现复杂）；②源代理接口按直连源 DR 还是直连源处理——结论方案二（直连源，双归组网不冲突）；③表项标记方案——复用 SPT+LOC 为主、评审"可以再加上新标记"（SPROXY），避免所有 SPT/LOC 使用点逐一加新标记易遗漏；④undo 后 RPT 误配置（一半 SPT 一半 RPT 无法自愈）恢复方案——开发建议方案一手动恢复，评审结论方案三：表项删除重建，借立即出接口列表计算逻辑判断 (S,G) 的 immediate_olist=joins⊕pim_include−lost_assert 是否满足 SPT，并新增需 NSR 备份的源代理标记位。
- **代码量**：总 2200 行（新开发 200 行 移植 2000 行），C 语言，14 个功能点（命令行前台注册/解析/ACL 检查/后台/NETCONF、pim 各模式修改、SM/DM/SSM 基本功能、SM 清除 SPT 功能含标记位、SM/DM/SSM 到源没路由适配、分支间代码差异），单点 LOC 100~300、难度系数 0.7~1.2；整体难度系数 1.08（精确值 1.08129807692308，用户态占比 100%、配置插件占比 20%）；整体难度与复杂度：中，周边模块影响度：中，验收测试工作量：中，风险：中，人力投入 1 人（开发林方钰 ys3904 设计刘海丹 09104，S 类新 V 模型，现有模块增强）。

### 14. 中金所公平组播（`multicast forwarding fair enable`）｜ SR88/B75 ｜ 1100 行（新开发）+ 微码配合
- **背景**：组播复制转发按出接口顺序进行（平台出接口列表按接口索引排序、稳态顺序固定），(S,G) 出接口 IF1/IF2/IF3 时每包总是先 IF1 后 IF2 再 IF3，排后叶子时延不公平；公平组播让各出接口逐包轮询先发机会（包2: IF2-IF3-IF1、包3: IF3-IF1-IF2 循环），使接收者时延相对公平。需求来自中金所（2025 年 6、7 月招标，中标后三个月开发周期，优先 25R2 启动），各大交易所均有需求，路由器空间 50-100 台；对标华为 NE40E V800R022C10 `multicast fair-forward enable`（HW 仅在 IPv4 章节描述，命令行是否 IPv4/IPv6 共用存疑），涉及平台/驱动/微码三层协同。
- **功能**：系统视图新增 `multicast forwarding fair enable` 与 `ipv6 multicast forwarding fair enable` 两条独立命令（PIM 进程 IPv4/IPv6 独立、可单独裁剪），undo 恢复缺省，用户角色 network-admin/mdc-admin，支持 commit/NETCONF。命令行注册在 MRIB，MRIB 保存配置并 DBM 备份；同步链路 MRIB→主控板 MFIB（保存数据并下驱动、驱动返回产品信息）→主控 MFIB 将配置及产品信息发各接口板 MFIB→接口板用产品信息下驱动。驱动接口在 IPv4/IPv6 组播驱动接口结构中各新增公平转发下发回调（命令类型取使能/去使能，驱动上下文参数由主控生成带给其他板）；驱动有公平配置则向微码下广播表携带标记，微码按标记在出接口间逐包轮询。规格：微码 MID 计数器 128k 直接作为公平组播规格，覆盖现有表项（IPv4 32k、IPv6 8k、IPv4+IPv6 32k，二层约 20k 独立，总 32k+20k），当前不超规格；未来表项超限时仍下发成功但共用计数器无法精确轮询，届时改驱动动态分配计数器、超规格表项不带标记降级。范围：不支持软转；BIER/NG-MVPN 公网表项不下发标记（带 BIER 隧道的私网表项正常带标记仍可公平转发）；纯二层组播不支持、三层 VLAN 虚接口下二层转发支持；BRAS 组播支持；MDC 支持管理/用户 MDC 配置并按 MDC 下驱动（SR88 暂不支持 MDC）。运维：不支持的板驱动返回不支持，平台输出 4 级 syslog 告警（参数为地址族），配置仍成功；返回失败起定时器重刷驱动；评审定方案一：仅输出告警不输出 clear；SNMP trap（hh3cIpMcastNotSupportFairForward）与 NETCONF event 产品确认不开发。display system internal multicast record 新增 fair enable 操作记录与 set-fair-fwd 过滤参数（诊断命令不按产品定制控制）；NETCONF IPv4/IPv6 组播全局配置表各新增公平转发列（enable/disable）。定制：命令行插件与 XML 插件的 MRIB/mroute 配置文件增加 IPv4/IPv6 公平转发定制项；simware 只下驱动不实现（主控返成功、接口板返不支持便于测试）。配置影响：仅新建表项走公平转发，已建表项需 reset（命令行提示）；跨芯片仅刷新侧变公平可接受。MRIB 全局配置结构与 MFIB 按 MDC 下发的数据结构各新增 2 字节；下驱动在内核态，用户态+内核态分布式同步，HA 为 M3、备份配置、ISSU 兼容。
- **难点**：平台/驱动/微码三层协同（平台配置同步与内核态下驱动、驱动广播表携带标记、微码借 MID 计数器逐包轮询）；128k MID 与组播表项规格匹配及未来超规格"驱动动态分配、超规格不带标记"降级设计；主控/接口板 MFIB 同步与驱动上下文参数传递；驱动返回值分类处理（不支持→syslog、失败→定时器重刷）；告警 clear 方案评审决策（方案一仅告警 vs 方案二告警+clear 需板间消息）；友商 IPv4/IPv6 命令行共用疑点的分析拆分。
- **代码量**：总 1100 行（全新开发、C、非移植），9 个功能点：命令行前台注册 100（难度 0.7）、前台解析 buildrun 100（0.7）、命令行后台 100（1.0）、NETCONF 200（0.7）、配置下发 MFIB 100（1.1）、MFIB 处理 100（1.2）、下驱动 200（1.2）、下驱动记录及显示 150（1.0）、告警 syslog 50（1.0）；综合难度系数 1.022（1.02227192307692），用户态占比 60%、配置插件占比 20%，涉及多线程/分布式/数据备份与复杂组网；整体难度与周边影响低，人力投入 1 人，验收测试工作量与风险较低。另 SR88 微码组几百行配合（独立起项目，预计 8 月 8 日联调、最迟两周后验收），SR88 驱动改动不大、问题单配合；SOW 撰写 20 人时。

### 15. MSDP 认证支持 TCP-AO（2025 南网测试）｜ V9 园区核心 ｜ 1000 行（新开发）
- **背景**：2025 南方电网"内生安全"测试（需求编号 202512160019-3，标题即"设备不能满足协议认证的测试例要求"）。MSDP 原有两种认证均不达标：MD5（`peer password cipher|simple`）算法简单，且密钥与应用程序一对一静态绑定，定期换钥需在两端多个应用上逐个手动更换；Keychain 采用 Bonica 草案实现、未标准化，仅能与同厂商或部分异构设备互通。对标 H 友商基于 RFC5925/5926 标准实现的 TCP-AO（每个 Key 可选更安全算法、多 Key 按活跃时间自动轮换、换钥不断连、SNE 防重放），并实现异构互通。项目类型：新 V 模型 S 类，现有模块增强，运行于 CEN/MPU 用户态、不涉及内核态、不加进程。
- **功能**：MSDP 视图（含 VPN 实例视图）新增 `peer peer-address tcp-ao tcp-ao-name [ accept-non-ao ]` 与 `undo peer peer-address tcp-ao`（tcp-ao-name 为 1～63 字符、区分大小写），与同一 peer 的 password/keychain 互斥：命令行注册用新函数、独立命令行消息结构，后台做互斥检查；commit 模式在前台经命令树查找按 peer 地址拼装 "peer x.x.x.x password / keychain / tcp-ao" 模糊查找冲突并打印扩展后的互斥提示（"password, keychain and tcp-ao cannot be configured simultaneously"）；tcp-ao 尚未创建时也允许下发（与 BGP/LDP 一致）。TCP-AO（MKT，Master Key Tuple）管理：每条 TCP-AO 经 `binding keychain` 绑定一条 Keychain，以 key-id + send-id/receive-id 关联其 Key；每 Key 独立支持 MD5、SHA-1、HMAC-MD5、HMAC-SHA1-12、HMAC-SHA1-20、HMAC-SHA-256、SHA-256、SM3、AES-128-CMAC 共 9 种算法，活跃时间支持绝对与周期（每日/每周/每月/每年）两种模式；TCP 选项中 1 字节发送/接收 KeyID 与 RNextKeyID（取值 0～255）标识收发 Key，按活跃时间自动换钥，配置容忍时间后真实活跃时间=容忍+原活跃+容忍，换钥平滑过渡不断连不丢包；MAC 按 RFC 截短至 96bit（12 字节）携带。防重放：32 位 SNE 序列号扩展与原 32 位序列号仿真 64 位序列号空间，防 TCP 长连接序列号翻转重放。实现上 MSDP 仅需 `setsockopt` 下发 TCP-AO 名称与 accept-non-ao 标志，已建连 peer 变更认证即断开重连；accept-non-ao 使对端未配 TCP-AO 时仍可建连，评审采用方案三：由协议模块实现该参数并向 TCP 下发选项（BGP 下发"不允许"），达到同一 TCP-AO 下 accept-non-ao 对 BGP 不生效、对 MSDP/LDP 生效（对应友商 accept-mismatch enable 仅 LDP/MSDP 支持的限制）。高可用：NSR 备升主时重设 socket 选项（同 keychain）；HA M3 模型、HA 通道批备、备进程收全后置 running；升级影响为 ISSU 兼容。NETCONF MSDP/Peers 表新增 TCP-AO 名称列（1～63 字符）与 accept-non-ao 列（enable/disable 枚举，依赖 TCP-AO 名称列、单独下发给出提示，与 keychain/password 两列互斥，get 缺省不填充）。display msdp peer-status 的 Peer authentication 字段扩充 TCP-AO 类型。MLAG/vSys 天然支持，新增命令需定制 CFS 双机配置同步规则并适配 commit、XML2CLI。
- **难点**：accept-non-ao 生效范围设计——评审比选"TCP-AO 统一实现不分协议""实现但对 BGP 不生效""直接在协议模块实现"三方案，最终采用方案三，利用 TCP 可按协议区分的特性由业务模块下发 TCP 选项控制，兼顾 BGP"全有或全无"的安全观与 MSDP/LDP 密钥切换窗口内保连通的运维需求；安全风险项方案——TCP-AO 只关联 Keychain 中部分 key，其风险不完全等同于所绑 Keychain，且同模块单条风险项下枚举方案在认证方式增多时排列组合爆炸，最终评审结论取方案 1（TCP-AO 不额外记录风险项、与友商一致），原有 MD5 弱算法提示与风险项显示维持不动（方案 2）；跨组依赖：OS 部门 TCP 模块需支持 TCP-AO 不匹配仍接受（accept-non-match）选项（OS 项目 NV202607130007，另定验收节点）。
- **代码量**：总 1000 行（新开发 1000、移植 0），C 语言、用户态 100%：tcp-ao 命令行注册解析 buildrun 300 行（难度 0.8）、命令行后台处理 200 行（0.8）、MSDP 连接支持 tcp-ao 100 行（难度 1.1）、netconf 支持 300 行（0.8）、显示命令及原有提示信息扩充 100 行（0.8）；整体难度系数 0.91，整体复杂度/周边模块影响度/验收测试工作量/风险均为较低，人力投入 1 人。

### 16. 配置安全-组播（MSDP Keychain 认证，人保测试）｜ S10500V7 ｜ 2100 行（新开发）
- **背景**：人保测试配置安全需求（需求编号 202405080075-6）的组播子项，属 Comware V7 现有模块增强、新 V 模型 S 类项目，对标 H 友商（HW）peer keychain 规格。MSDP 原仅支持 `peer peer-address password`（MD5 加密），算法安全性低且无更优替代，风险无法解除；安全大项目引入双模式框架——弱安全模式（对应 HW WEAKEA 包已安装）风险配置允许下发但输出提示并记入 `display security risk` 风险项，强安全模式（`enhanced-security mode enable`）弱安全配置直接失败不下发；弱切强时若存在风险项则切换失败，需用户 display security risk 查看并手动删除风险配置。验收产品为园区核心交换机 S10500V7 及 simware。
- **功能**：新增 `peer peer-address keychain keychain-name` 与 `undo peer peer-address keychain`（公网/VPN 实例 MSDP 视图，配置级，keychain-name 1-47 字符），与同一 peer 的 password 命令互斥——立即/后台模式通过会话配置中的 keychain 名（最长 63 字符）与 password 字段判互斥，commit 模式前台用命令树查找接口按 peer 地址拼装 `peer x.x.x.x password/keychain` 模糊查找；命令行注册独立函数与独立消息结构、支持 commit/netconf/XML2CLI。MSDP peer 基于 TCP，建连时拷贝 keychain 名后经 socket 选项下发给 TCP 即可，TCP 模块响应 keychain 事件，已建连 peer 配置/修改认证时连接断开重连。keychain 支持 7 种算法，TCP 仅支持 5 种（MD5/HMAC-MD5/HMAC-SHA-256/SM3/HMAC-SM3，即 TCP 增强认证处理的 5 类算法），SM3/HMAC-SM3 需加密库算法信息查询成功（非 FIPS 算法类型）且 FIPS 模式未使能才支持。NETCONF MSDP/Peers 表新增 keychain 名列（1-63 字符、与 password 列互斥，get 缺省配置不填充）。配置安全交互：password 配置成功（commit 视图需冲突检查通过后）打印 insecure 算法提示并调用风险项接口记录风险（框架以 DBM 全局运行数据库存储、支持主备备份；首条风险触发风险项产生日志与 hh3cSystemSecurityRiskOccur TRAP，全部删除触发清除日志与 hh3cSystemSecurityRiskClear TRAP）；强安全模式下 password 命令行安装流程借助 check/help 回调查询强安全模式状态使 password 不联想不可配，netconf 流程经强安全模式判断返回错误码（错误信息为新增）。风险按 MD5/keychain 分别计数，仅 0→1、1→0 临界变化时增/删风险项：两者皆有计数时记录汇总风险项（同一风险子类型以最后一次调用为准）；keychain 侧经模块初始化注册 keychain 事件回调，仅处理弱安全 key 状态位（0x08）事件，判定所绑 keychain 含弱安全 key 后增/减风险项。高可用与异常：板插入按强弱模式复用命令行流程（强安全删弱配置、弱安全加计数），板拔出删风险项；备升主时非 NSR 走 DBM 恢复风险计数，使能 NSR 时主备进程均经 NSR 备份消息计数但仅主更新风险项（全局 DBM），备升主后按计数更新或删除风险项保证一致性；配置恢复与命令行流程共用自动更新风险项。运维显示：display msdp peer-status 参照 HW 增加 Peer authentication（none/MD5/Keychain，接口体新增认证类型字段）；display system internal msdp statistics 增加弱安全算法计数（Insecure algorithms on peer(MD5/keychain)）、最近风险项操作及 add/add-failed/delete/delete-failed 累计统计（复用既有统计接口结构，主降备或 reset 清 0）；需在公共初始化的风险显示配置中添加 MSDP 模块与其命令行插件的映射项才能按模块显示风险。
- **难点**：双模式下 CLI/netconf 行为矩阵不一致（弱安全 CLI 成功+提示+风险项、netconf 成功无提示；强安全 CLI 不可联想、netconf 返错），需分别在前台解析/help/check 回调与 netconf 处理链路接入模式判断；commit 视图互斥检查只能前台模糊查找、无私有数据可用；风险计数仅临界 0→1/1→0 增删且 MD5 与 keychain 同时存在时要改写为汇总风险项，add/del 失败路径需分别累计统计；NSR 场景下主备风险计数与全局 DBM 风险项的一致性（备进程计数但不更新风险项，备升主后按计数重建）；keychain 弱安全 key 0↔1 事件需遍历全部 peer 配置匹配 keychain 名再联动风险项；TCP 仅支持 7 种算法中 5 种且 SM3 依赖产品定制与 FIPS 状态；系统测试需覆盖 keychain 与 password 间配置回滚。
- **代码量**：总 2100 行（全部新开发、C 语言、非移植，不涉及内核态/新加进程，用户态占比 100%），11 个功能点——keychain 命令行注册解析 buildrun 300 行、命令行后台处理 300 行、MSDP 连接支持 keychain（IPv4/IPv6）100 行、netconf 支持 300 行、显示认证类型 100 行、password 前台提示风险 100 行、前台帮助检查控制 100 行、后台加计数并更新风险项 200 行、keychain 事件处理 300 行、备升主等事件处理 200 行、显示统计计数 100 行；整体难度系数 1.012921875（配置插件占比 10%，MIB/命令行/Netconf 用户态普通难度代码占比 10%）；人力投入 2 人（开发付惠泉 22570、林方钰 YS3904，设计负责人刘海丹 09104），整体难度/周边影响/验收测试工作量/风险均为"较低"。

### 17. Videlco IGMP Plus 补充场景（igmp-snooping NETCONF）｜ B70D064SP38XX ｜ 2500 行（新开发200 移植2300）
- **背景**：客户 Videlco 对标 NETGEAR IGMP Plus（WEB 一键下发 NETCONF，set igmp-plus 101 一条命令等效多条 igmp-snooping 配置），全局 fast-leave 原不支持 NETCONF。现场测试补充：二层组播默认向路由器端口转发，客户链路带宽优先、用户不点播时不转发，故需 VLAN 视图 igmp-snooping router-port-discard 的 NETCONF（含 MLD 对应命令）。需求自 sp34XX 同步至 sp38XX，S 类简单+移植项目，不涉内核态、不加进程。
- **功能**：① IGMPSnooping/MLDSnooping 全局 Configuration 表新增 FastLeave（fast-leave 开关）与 FastLeaveVlanList（fast-leave VLAN 列表）两列，评审定为方案一（set 必带 FastLeave、get 两列必带），否决方案二"仅全局 fast-leave all 布尔列"与方案三新建 View 索引表；FastLeaveVlanList 形如 1,2,3,5-8,10-20，支持 base:incremental 增量 create/merge/delete/remove，replace 增量被框架拦截。② VLANs 表新增 RouterPortDiscard（router-port-discard）列（缺省 disable）。③ 补齐 entry-limit/global-enable/host-aging-time/host-tracking/last-member-query-interval/max-response-time/router-aging-time 共 7 条全局命令 NETCONF（MLD 侧对应 last-listener-query-interval）。④ XML2CLI："1,2,3,5-8,10-20" 与 "1 to 3" 互转需回调函数把 bitmap 转 vlan 列表并区分增量，Enabled 列设回调输出 igmp-snooping 视图。⑤ XML 配置框架与 MCS 进程交互改造：get 消息结构仅加简单类参数、不含 vlan bitmap（否则后续无法再加字段，保 ISSU 兼容），fast-leave bitmap 经独立位图 TLV 下发；set 复用命令行消息（全局使能类消息等）；插件经增量编辑判断接口判增量。⑥ gRPC/RESTful 无需特殊修改（RESTful 无索引全局表 put/delete 不支持），YANG 仅改文件；自 B70D034SP 移植 B70D038SP 无差异；交付 NETCONF/RESTful/gRPC API 参考、YANG 文件、测试用例/报告、特性说明书。
- **难点**：① 全局与接口 fast-leave 语义差异：接口侧不带 vlan 与带 1 to 4094 等价，NETCONF 必带 vlan list，仅发索引列报错。② vlan list merge 语义不统一：igmp-snooping/L2VPN 为并集，VLAN 模块为替换，带不带 incremental 结果不同，评审选与本模块看齐的方案一。③ xml2cli 无法完全精确：错误下发时框架将 remove 改为 merge，replace 先 undo 全部再重配、非仅 undo 差异，已确认 xml2cli 仅供人看（系统部评审）。④ ISSU：与 MCS 交互的 issu 结构新增成员，靠独立 TLV 保兼容。⑤ 已知缺陷：增量 delete 时 get 不返回 FastLeaveVlanList 值致误报配置不存在。
- **代码量**：总 2500 行（新开发 200 移植 2300），12 个功能点：fast-leave 前台 400/后台 100/xml2cli 200 行（难度 0.9），7 条全局命令与 router-port-discard 各 200 行（0.7），移植适配 200 行，均 C 语言；难度系数 0.8375，整体难度较低、人力投入 1 人；配置插件占比 20%，MIB/命令行/Netconf 用户态普通难度代码占比 30%，用户态占比 100%；主模块影响 MCS；开发林方钰，设计刘海丹，验收产品 S6520X、S6530X、S7500X-G。

### 18. Videlco IGMP 协议及 WEB 界面开发（igmp-snooping fast-leave NETCONF）｜ B70D064sp ｜ 2100 行（新开发）
- **背景**：Videlco 交换机项目 IGMP 协议及 WEB 界面开发（需求编号 202403150112-1，V7 特性，现有模块增强，新 V 模型 B 类简单项目，验收产品 S6520X-SI/EI/HI、S6530X、S7500X-G）。NETGEAR 交换机支持 IGMP Plus 功能，执行一条 `set igmp-plus 101` 即可完成 igmp-snooping 部署；对应我司需逐条配置 `igmp-snooping` 视图下 `fast-leave vlan 101`，及 vlan 101 下的 igmp-snooping enable、drop-unknown、querier、query-interval 30 等多条命令。本项目对标该一键部署体验：在 WEB 上一键 IGMP+ 触发上述命令的 NETCONF 下发（后台仍按多条配置处理），并为原本不支持 NETCONF 的 IGMP-Snooping 视图全局 fast-leave 命令补齐 NETCONF 能力，同时补齐其他全局简单配置的 NETCONF。主模块为 MCS（影响 igmp-snooping、mld-snooping 全局表 netconf），全部用户态实现，不新加进程、不涉及内核态。
- **功能**：①全局 `fast-leave [ vlan vlan-list ]` NETCONF：在 IGMPSnooping/Configuration 与 MLDSnooping/Configuration 全局表新增 FastLeave（enable/disable）与 FastLeaveVlanList（形如 1,2,3,5-8,10-20）两列，评审选定两列并存的方案一；set 操作必须携带 FastLeave 列，get 操作两列必须同带，否则获取结果可能不全。②WEB 一键 IGMP+：一键触发 IGMPSnooping/Configuration（Enabled=enable）与 IGMPSnooping/VLANs/VLAN（VLANID=101，使能/drop-unknown/querier 置 enable、query-interval=30）等 NETCONF 下发，等价于 fast-leave vlan 101、VLAN 使能、drop-unknown、querier、query-interval 系列命令。③7 条全局简单命令补齐 NETCONF（IGMP/MLD 双表同步新增列）：entry-limit（0~4294967295）、global-enable、host-aging-time（1~8097894 秒）、host-tracking、last-member-query-interval（1~25 秒，MLD 对应 last-listener-query-interval）、max-response-time（1~3174 秒）、router-aging-time（1~8097894 秒）。④vlan list 增量下发：merge 语义看齐本模块接口下 fast-leave 表（vlan 1 再 merge 2-3 结果为 1-3，与 L2VPN/SRVs 表一致），并新增 base:incremental 增量下发（create/merge/delete/remove 支持，replace 由框架拦截返回失败），实现删除部分 VLAN。⑤XML2CLI：IGMPSnooping/Configuration 表原先不支持 xml2cli，本项目支持；因 NETCONF 可不带 Enabled 列下发正向配置，由 Enabled 列的回调函数决定输出 igmp-snooping 还是 undo igmp-snooping 视图；FastLeaveVlanList 的 NETCONF 形式与 CLI 形式（1 to 3 5 to 8）不同，仅改 tree.xml 无法完成转换，需回调函数将输入 bitmap 转为 vlan 列表并区分是否增量下发（参考 VLAN trunk 接口表的既有 xml2cli 处理）。⑥gRPC/RESTful/yang 无需修改代码即可支持（RESTful 无索引列全局表不支持 put/delete；yang 仅改 yang 文件）。交付 NETCONF/RESTful/gRPC API 参考、YANG 文件、系统测试用例/报告、特性说明书、代码静态检查报告等。
- **难点**：①全局表 Enabled 列 xml2cli 三方案对比：因 NETCONF 可不带 Enabled 列下发正向配置、反向删除时带 Enabled 列会停止 MCS 进程导致报错，xml2cli 无法做到完全准确。方案一为 Enabled 列设置 xml2cli 回调函数，按是否 undo、是否携带 Enabled 输出 igmp-snooping 或 undo igmp-snooping；方案二要求 Enabled 列必带（不可行：删除视图下配置时带 Enabled 列应理解为删除 igmp-snooping 视图，语义不合理）；方案三新增视图配置新表、以视图列（枚举仅 igmp-snooping 一个值）为索引列（与原表功能重叠、相当于废弃原表）。开发建议方案二，评审结论定为方案一（回调函数方案），兼容原表。②vlan list merge 语义两方案对比：方案一跟本模块现有 igmp-snooping 表看齐（merge 累积，不带/带 incremental 在不报错时结果一致，不易出问题），方案二跟框架 VLAN 表看齐（merge 替换，带不带 incremental 结果不同，易出问题）；开发建议与评审结论均为方案一。③消息改造与 ISSU 兼容：get 类操作的消息结构仅新增简单类参数字段、不增加 vlan bitmap 字段（否则后续无法再追加字段，影响 ISSU 兼容），bitmap 经独立位图结构作为第二个 TLV 发给前台（前台框架支持多 TLV，简单适配即可）；set 类新增列复用命令行消息（如全局使能类消息），后台与命令行复用处理流程；插件侧通过增量编辑判断接口判断增量下发并按操作类型分别处理。
- **代码量**：总 2100 行（全新开发、非移植，C 语言）：fast-leave NETCONF 前台 400 行、后台 100 行、fast-leave xml2cli 200 行（难度系数均 0.9），entry-limit/global-enable/host-aging-time/host-tracking/last-member-query-interval/max-response-time/router-aging-time 7 条命令支持 NETCONF 各约 200 行（难度系数 0.7）；评估难度系数 0.837475961538462（约 0.84），整体难度与复杂度较低、周边模块影响度低、验收测试工作量小、风险较低；人力投入 1 人，用户态占比 100%，配置插件等套路代码占比 20%、用户态普通难度代码占比 30%。

### 19. 丢弃 FF10 前缀（scope 0）IPv6 组播报文｜ 12500X-AF/HPE 12900E（K 系列单板）｜ 1500 行（新开发）
- **背景**：现网组播可正常处理 FF*0（scope 0）IPv6 组播报文（含静态组），但 scope 0 属保留值需丢弃；此前仅临时方案，正式化恐影响现网。表项涉及 MLD/PIM6/MFIB，既有过滤（group-policy、source-policy 等）无法按 FF*0 前缀统一限制；含 FF*0 报文有 MLD report、PIM join/数据流。评审在"仅限 MLD/MFIB"（PIM6 与 source-policy 不一致）与"MRIB 集中管控三模块"间采纳后者。
- **功能**：新增系统视图命令 `ipv6 multicast deny scope scope-id`（scope-id 仅 0，undo 恢复缺省），禁学/禁转/禁生成 FF*0 报文，"一禁即禁"；支持 commit（新增消息号 1700）与 display this all。MRIB 集中管控：全局配置增加禁学 scope 字段并发布新事件，PIM6/MLD 注册关注，经 MRIB 通知消息同步两者；新增 MFIB 路由类型枚举值（追加在枚举末尾保 ISSU 兼容）通知 MFIB。过滤检查点：MLD/PIM6/MFIB 三处（各自组播过滤入口）新增 FF*0 检查，与 source-policy/group-policy 冲突时任一禁止即禁止。PIM 记录配置变化标记，VRF 15 秒周期定时器超时通知 MLD/MD 重报加入并打删除标记，覆盖 SM/DM/SSM/双向。MLD 侧将策略存入全局配置，主线程转 EVENT 线程分批遍历所有 VRF 接口：限制时删 FF*0 静态+动态组及源，undo 时仅恢复 FF*0 静态组生效；MFIB 仅记录状态随 PIM/流变化。平滑：MLD 向 MBR 请求配置，新增消息插在 SSDP 后、ssm-policy 前，MBR 回发并结束平滑。HA：M3 模型，MRIB 处理 MBR 消息时经 NSR 备份给备进程。
- **难点**：undo 区分静态组（重新生效）与动态组（等重报），分批遍历防大表项阻塞；V9 无内核态，MFIB 线程在转发进程；多线程/分布式/数据备份，难度系数 0.827884…。
- **代码量**：总 1500 行（新开发 1500）：命令行 500（难度 0.7）、MRIB 150、MFIB 50、MLD 350、PIM 250、平滑 150、HA 50（其余难度系数均 1），C 语言、非移植；难度中、周边影响低、人力 2 人、验收测试中、风险中。

### 20. 组播一体化BRAS（可控组播）V9 支持 ｜ CR19K-X Comware V9 ｜ 11200 行（新开发3200 移植8000，折算4630）
- **背景**：BRAS 组播即"可控组播"——宽带接入服务器上按用户（PPPoE/IPoE/Portal，domain 配置 service-type stb 即通知组播）对组播点播做认证、复制与隔离：普通组播出接口目的 MAC 为组播 MAC、同接口用户互可见，可控组播为每个上线用户分配虚拟 MUA 派生口（目的 MAC 为用户单播 MAC），实现每用户隔离复制，并经 access limit、频道列表、user-profile 的 igmp access-policy 多重控制。该主体 2014 年首次合入（需求单 202508160002-37），SR88/B75 分支已有实现，V9（CR19K-X）此前仅合入代码、未打开定制（igmp/mld 配置文件的 --auth/--Defense 定制项、libmuser.so 交付件定制）；动态 VLAN 复制对标华为 NE40E 的 copy by-vlan 复制模式（按两层 VLAN 确定转发份数，HW 共四种复制模式可同时配置），显示统计能力对标华为 ME60/NE40E 的 `display multicast group-ip`（按组+出接口查点播用户及 Local/Remote 用户数）。
- **功能**（6 个子需求）：① BRAS 组播基础——移植 `igmp authorization-enable`（三层以太/聚合口及子接口、VT 口视图）、`igmp join-by-session [mode {both|bras|non-bras}]`、`igmp access-policy`（User-Profile 视图，ACL 2000～3999）、`igmp user-vlan-aggregation {dynamic|dot1q vid vlan-id [second-dot1q vlan-id]}`、`igmp static-group ... [dot1q vid vlan-list|dot1q vid vlan-id second-dot1q vlan-list]`（批量方式 inc-step-mask 可配 2～512 个组）、`igmp attack-defense`（仅 IPv4/IPoE，未开 IPoE 认证的接口会丢弃全部 IGMP 报文）；MUA 派生口/base 口模型与 base 口状态机（配置面同步派生口、满足条件创建运行口）；IGMP/PIM NSR；接入认证模块经引用接口打开 libmuser.so，取注册/去注册/上线/认证变更/下线/平滑开始/平滑/平滑结束等通知接口感知用户上下线（V9 接口增加认证类型枚举入参，覆盖 DOT1X/PORTAL/PPP/IPOE 等 10 类认证）；静态组 HA 消息结构重构（变长位图从静态组结构中拆出独立子结构、靠自描述长度区分新老结构，批量结构同改）保 ISSU 兼容。② UCM 统一用户管理适配——用户信息由 UCM 统一管理（Portal 除外），User-ID 不再含接入类型需查表获取，维护 Session-ID/Subscriber-ID 下发转发与驱动；PPPoE 配置从 VT 口迁移物理口（L2TP/LNS 仍留 VT/BAS 口），认证信息中四个接口索引（物理口/接入口/以太 PPP 口/VT 口）简化为三种；trace access-user 用户级跟踪。③ NETCONF 移植——IGMP/MLD Interfaces 表新增认证使能/按会话加入/聚合外层 VLAN/聚合内层 VLAN 四列，新增 IGMP/MLD UserProfiles 表（用户 Profile 名 1～31 字符、接入策略 ACL 2000～3999），并需支持 XML2CLI。④ 动态 VLAN 复制——igmp user-vlan-aggregation dynamic，MUA 口与用户改为一对多二级树（一级按 MUA 接口索引、二级按用户标签 UserLabel，首个用户上线创建/最后一个下线删除），mrib lib 按 User-ID 与 MUA 索引两棵树维护用户信息，过策略改按用户标签查授权；系统部评审结论：复制模式切换必须先强制所有 BRAS 用户下线，接口下有 STB 用户在线时不允许 undo by-session/dynamic 模式。⑤ 显示命令增强——display igmp user-info 新增 active-group/inactive-user/statistics 关键字（仅显统计头，如已授权/匹配/活跃用户数 in total），display igmp group 新增 statistics（动态/静态组数统计头，解决现场 1000+ 组刷屏且 include 过滤性能差），display igmp interface verbose 新增 Authorization/Join-by-session/User-VLAN-aggregation (Dynamic) 等行。⑥ 用户信息新字段下驱动——废弃用户 ID/账号 ID 旧方案，由 UCM 在用户认证信息结构中新增 UCM 索引与 16bit 哈希字段（与 UCM 下发路由一致）经组播出接口下发驱动 MLL，保证同一用户单播/组播报文出口一致；前后域切换、二次认证、1X+IPoE 认证时字段变更需通知组播先删后加。
- **难点**：IGMP/PIM NSR 大量运行数据实备与恢复（IGMP 备份动态组、生效静态组、base/派生口、用户上下线消息，PIM 备份路由表/接口状态/GenID/邻居/bsr/rp，备升主需重建配置口/运行口、恢复封装-MUA 对应关系及 QinQ/VSRP 临时 AVL 数据，IGMP 与 PIM 互相平滑）；含变长位图 HA 消息结构拆分的 ISSU 兼容；B75→V9 移植代码冲突逐行同步（与二层可控组播共用 lib、V9 的 MVXLAN proxy/dscp/ssdp deny/scope deny/m-lag 差异，仅 igmp 模块主配置文件约 1000 行、XML 接口 1200 行、用户接入认证 1450 行，新增用户 Profile XML 插件 780 行）；V9 全量字节序转换（B75 部分未转序）、下驱动改走 LRPC 远程调用转发进程（LIPC 长连接+共享内存）；同步 4 个主线问题单（温备 CUSP 震荡 core、commit 缺省配置显示、全局/接口配置支持、ssm-policy 配置残留）；1M PPPoE/512K IPoE 双栈会话规格下内存剩余 2G 以上的约束；不支持 M-LAG/RBM/VPP/软转（MBUF 无法设置用户 ID）。
- **代码量**：总 11200 行（新开发 3200 移植 8000，折算 4630），难度系数 1.147，用户态占比 90%（配置插件占比约 5%），3 人；整体难度与周边模块影响度中、风险中、验收测试工作量大，验收产品 simware；交付件含 YANG 文件、NETCONF/RESTful/gRPC 及驱动 API 参考、系统测试用例、命令参考/配置指导等。

### 21. MLAG 易用性可维护性优化（组播）｜ V7（S68/S10500v7/S5560X）｜ 1300 行
- **背景**：源自客户需求电子流 202203148603-3【V7】MLAG 易用性可维护性优化（项目号 B70D064SP，S 类项目），负责其中组播部分，特性合入 68/10500v7/5560X（不裁剪）。M-LAG/EVPN M-LAG 组网下三层组播命令同步规则复杂：有的命令两端必须一致、有的必须不同、有的可同可异，漏配会导致动态 RP 学习不全、注册/加入无法汇总、组播流量不通，或 M-LAG 两侧 VLAN 虚接口竞选 DR 后仅一侧转发流量。命令同步与检查提示的框架由 LAN 组配合项目【V7】MLAG 易用性可维护性优化（LAN 组开发中）提供（易用性检查依赖该项目框架，配合组启动项目），本项目负责 PIM/MCS/MFIB/MVXLAN 等组播模块接入。对标 HW：要求组播配置两端一致且用户侧接口配 PIM Silent（对应我司 pim passive，可用 pim distributed-dr 替代），不支持 MVXLAN m-lag 特性与 PIM Snooping（我司支持）。
- **功能**：①命令同步规则梳理（改同步规则 conf 文件，业务代码零改动但需各模块测试验收）。可同步（可能相同或不同）：PIM 视图 anycast-rp anycast-rp-address member-address、c-bsr ip-address [scope …][hash-length|priority]*、c-rp ip-address [advertisement-interval|group-policy|holdtime|priority]* [bidir] 及 IPv6 PIM 视图对应三条（anycast-rp/c-bsr/c-rp 的 IPv6 版）；MRIB 系统视图 ip/ipv6 rpf-route-static [vpn-instance …]（组播静态 RPF 路由，双归接入时两端相同、单挂接入时不同）；MSDP 视图 originating-rp interface-type interface-number、peer X connect-interface/description/password {cipher|simple}/mesh-group、static-rpf-peer [rp-policy]、shutdown peer-address（手动关 Peer，两侧同关才同步、只关一侧不同步）。对称配置（一定不同）：multicast-vpn vxlan m-lag local X remote Y（系统视图）与地址族视图 m-lag local X remote Y——IPL 与 Keepalive 双故障时隧道源切换为本地地址，remote 用于识别对端 SBD-SMET/S-PMSI A-D 路由；三种配置优先级为地址族 m-lag local > 系统视图 multicast-vpn vxlan m-lag local > evpn m-lag local。不同步：mtrace v1/v2/v2 ipv6（可执行命令非配置）、multicast-vpn vxlan edge remote（多 ED 组网指定同 DC 其它 ED 地址识别其 S-PMSI A-D 路由，3ED 时每台配两条，仅在使能 dci enable 时检查提示）。②配置缺失检查提示（10 项）：框架提供注册接口，用户输入检查命令时各模块在命令行插件注册的回调中向进程发 Check 消息，插件侧向 app 获取配置缺失后返回框架显示。MVXLAN 5 项：source 未携带 evpn-mlag-group 关键字提示需携带；source 接口地址与 evpn m-lag group {virtual-vtep-ipv4|virtual-vtep-ipv6} 不一致或无地址提示"主地址需要和 evpn m-lag group 一致"；m-lag local 按全局/地址族配置情况分四种提示（全缺→需配 evpn m-lag local；部分地址族缺→部分未配 m-lag local；已配 multicast-vpn vxlan m-lag local 或地址族全配→建议改配 evpn m-lag local）；使能 dci enable 但未配 edge remote 提示需配置。PIM 3 项：VLANIF 使能 PIM 且 VLAN 内有 M-LAG DR 口→检查对应 VLAN 是否使能 pim-snooping；VSIIF 使能 PIM→不论有无 DR 口一律提示使能 pim-snooping（EVPN 精确转发需求）；IPL 逃生 VLAN 提示（在易用性分支不支持、移主线实现）。IGMP 2 项：igmp-snooping 的 VLAN/VSI 同构检查。事件感知：MVXLAN 关注 L2VPN 的 EVPN 组地址变化事件，记录 evpn m-lag group 地址（有则视为 M-LAG 系统，source 增删时模拟配置/去配置流程）；PIM/IGMP 注册 VLAN DR 竞选变化事件（首次加入/最后离开结构，携带变化 VLAN 位图）维护有 DR 口的 VLAN bitmap 并在主线程更新到接口参数；snooping 使能情况从 MFIB 获取。③配置简化（方案设计与评审，该需求点在 IPL 逃生项目中落地编码，本项目不涉及）：M-LAG 下 VLAN 虚接口（使能 pim sm/dm 且 VLAN 内有 DR 口）免配 pim distributed-dr/pim passive（IPv4/IPv6 四条命令），在 DR 判断处（邻居选举与协议查询两个入口）查询 VLAN 内是否有 M-LAG DR 口即视为满足 DR 条件，条件变化时重走 DR 选举；备选方案一仅提示、方案二自动下发（存在自动/手动配置区分及"满足→不满足→满足"时序难题）、方案三查询 DR 口，评审结论取方案三。MVXLAN source/m-lag local remote 简化两方案：方案一 source 配本地实地址并经用户态 RLINK 通道同步对端（RLINK 消息含 VPN 名与 source 地址，支持批量通知、老化、断链重连平滑，配置与自动学习冲突时配置优先）；方案二 m-lag local remote 直接听从 evpn m-lag local 配置（需 L2VPN 支持在 ED 配置），评审结论取方案二。
- **难点**：DR 语义重构——协议规定收到 IGMP 加入后仅 DR 口可添加组播转发表出口，而 M-LAG 两侧 VLAN 虚接口不能竞选 DR、须都作为出口（最终按组地址奇偶规则仅一侧实际发出），故不能简单放行，需按 DR 判断的调用处分四类场景处理：仅显示（SM/SSM/Bidir/DM 四类接口显示及四个 OIF 显示函数共 8 处，按实际情况显示不做特殊处理）；本地直连源触发向 RP 发送注册（注册触发、注册定时、表项合并、上游切换、MSDP 通知等约 11 处直连源 DR 判断）；出口添加（VLAN 内有 M-LAG DR 且接口使能 igmp/mld 即视为满足）；DF 竞选（仅双向 PIM 场景，M-LAG 未支持双向 PIM 暂不特殊处理）。级联 M-LAG 必须配 pim distributed-dr 而非级联两者皆可，也需区分。公共需求：支持 ISSU、支持 IRF、支持进程亲和性（PIM/MD），license 不涉及。
- **代码量**：MVXLAN 命令检查提示 400 行 + PIM 检查提示 500 行 + IGMP 检查提示 400 行 = 1300 行（C，非移植，责任人 Yangjie 05534），难度系数 0.88（评估表精确值 0.883961177884615），用户态占比 70%；多线程/分布式为 YES、周边模块简单配合、无互通/复杂组网验证，MIB/命令行/Netconf 用户态普通难度代码占比 50%、配置插件占比 0%；整体难度与复杂度中，周边模块影响度低，无新增命令行（仅新增 M-LAG check 提示检查）。


## 四、高端路由器 BIER FRR 19K-X 适配

### 22. MSR6 BE 支持 FRR-MCAST（移动新技术 2023）｜ CR/SR88 B75 ｜ 9.7K LOC
- **背景**：移动 2023 新技术标准牵引（需求 202304170073-2，72 页 SOW，新 V 模型 S 类＋紧急开发类，对标 H/R 友商、各厂家均处开发阶段）。双根 1+1 热备已支持，本期补齐温备双根 1:1 与 BIER 隧道本地 FRR：1:1 仅护源侧、MSR6 BE 域内故障靠本地 FRR，两者切换目标 50ms（与移动确认以实测为准）；明确不做前向兼容（不支持节点自动退 1+1）与跨域域间保护（依赖 BGP FRR）。主模块 MD/PIM/MFIB/BIERRIB/BIERFIB，配合 ISIS/BGP/FIB，验收 CR/SR88。
- **功能**：① 双根 1:1 温备（draft-wang-bess-mvpn-upstream-df-selection-07 主动协商）：UMH 路由新增 IDF 协商 BGP 团体属性（RFC 1997 格式，建议保留值 0x0000FFFF 待 IANA 申请），MD 经 Local source active 消息联动 PIM/BGP，VPNv4/v6 与公网 GTM 均支持；IDF 选举两种粒度——MVPN 实例粒度（本地/远端 MVPN ID 经 UMH 的 VRF Route Import EC 感知、排序取前二）与 (C-S,C-G) HASH 粒度（VAL=(1103515245*((1103515245*(C-G&M)+12345) XOR C(i))+12345) mod 2^31，IPv6 MVPN ID 四段异或折算 32 位，掩码 IPv4 前 30 位/IPv6 前 126 位）。命令：MVPN IPv4/IPv6 地址族视图 warm-root-standby [acl-number|name acl-name]（ACL 2000~3999，仅 BIER 模式，与 c-multicast frr 互斥）、全局 multicast-vpn df-selection source-group（缺省实例粒度）；NETCONF MVPN/Instances 下增双根温备类型与 DF 选举类型两个节点。备 IDF 向主 IDF 发 C-multicast 路由加入其 PMSI Tunnel 引流（混合 MOFRR：VPN 接口主 IIF＋隧道备 IIF），流量检测感知主 IDF 节点/路径故障；备 IDF 预置未激活备 BIER 隧道出口下发 MFIB 不下驱动，故障后激活下驱动升主、回切按路由/流量恢复处理；尾端按 Root PE 列表"任收"RPF 检查，主备入接口共存下驱动、不做流量检测。复用 multicast frr mode [by-entry|by-iif]（缺省按表项）、multicast frr monitor cycle（缺省 200ms，可 10/20/40/100/200/500）、multicast frr wtr {wtr-time|disable}（缺省 600s，60~1800s）。② BIER 本地保护：BIERRIB 接收 ISIS 主备邻居/下一跳（含备份 BFR-prefix、备份邻居节点 ID 与备份 BFR-ID 范围列表等主备字段；本期一主一备、框架预留多主多备），按单邻居故障先算全正常 ECMP 表再算故障表，得主 BIFT 与备份 B-BIFT（备份位掩码 F-BM/BF-BM，备份动作 BFA 取普通转发/显式路径（加 SRH 单播续转）两类，BEA 激活标志；备份 BIFT 可定制最多 2 邻居，ECMP 备份规格上限 8）；LFA（遍历 MSR6 直连邻居、无环条件 (N,D)<(N,E)+(E,D)、节点保护优先）与 TI-LFA（扩展 P 空间、Q 空间、收敛后 SPF、Repair List、备份出接口；Explicit 动作加 SRH 单播至尾节点弹 SRH 续 MSR6 转发）；故障检测：接口 Down＋BFD——BIER 子域视图 primary-path-detect bfd {ctrl|echo}（IPFRR 类型会话检测 VN 主下一跳）、primary-neighbor-detect bfd ctrl（BIERFRR 类型多跳 BFD 检测主 BFR 邻居，源/目的用 BFR-prefix，仅 ctrl）；VN 下发携带主 FRR、BFD echo、扩展 BFD ctrl、扩展 ECMP 备份、FRR 备份、SRv6 NHLFE 等标志；切换由 BIERFIB 激活 BEA、以 FRR 切换动作下驱动（切换消息含各备份 ECMP 数量与主动作 ID），BIERRIB 重算 BIFT 支持多点故障；正切防微环（PLR 延迟收敛定时器维持 TI-LFA 转发）、回切防微环（先收敛节点构造防微环显式路径加 SRH 单播）。跨域 BGP bfr-id range：PB 配代理汇总域内外 range、CR/PSR 不配，改用原始 ASBR2 前缀携带对端 AS range、End.RGB 用 ASBR1。显示 display bier routing-table [sub-domain|bsl|prefix|verbose]（NBRFlag/NhFlag "B" 标备份）、display system internal bier fib forwarding-table（FRR 1/FRR 2 分表）、display system internal bier rib neighbor；NETCONF BIER/Sub-domains 下增主路径 BFD 检测节点（取值 0/1 对应 CTRL、2 对应 ECHO）与主邻居 BFD 检测节点（取值 0/1 对应 CTRL）。
- **难点**：用户态/内核态协同（内核态完成双根 1:1 与本地保护的表项下发、切换转发，感知 BFD 故障快切；主备同步依赖 DBM、板间 LIPC）；跨域 BFR-prefix 代理成环（缺陷 202106081668，最终以"EBGP 两端不能同时配置 bfr-prefix 代理"约束解决）；备 IDF 经 PMSI Tunnel 引流检测主 IDF，主备间链路故障会误升主成双主双流，以多条传输隔离并行链路＋Underlay 本地 FRR 规避；尾端 SA 变化时序（缺陷 202307100985：MFIB 收 SA 地址更新消息时打刷新标记、后台定时器渐次刷新驱动）。难度/周边影响度/风险均"高"。
- **代码量**：总 9.7K LOC、31 个功能点（单点最大 500），C、无移植；用户态 40%、配置插件 8%、MIB/命令行/Netconf 普通难度代码 2%；难度系数 1.43187060288462（≈1.43），人力 4 人（设计负责人 yangjie、开发负责人 zhaojiangdi，开发含 guanyi/jinxin/zhaojiangdi/wangyarong/yangjie），涉及 public/public2/public3，SOW 撰写 80 人时，验收产品 CR/SR88。

### 23. 电信 STN 集采 BIERv6 移植-组播｜ SR88/CR B75 ｜ 5.1K LOC（全部移植）
- **背景**：电信STN集采要求SR88/CR（Comware V7 B75）支持BIERv6，与G-BIER实现基本一致，组播侧在既有G-BIER的NG-MVPN上移植适配、复用其代码，重点为源地址配置改用SRv6 locator+SID生成、BGP路由携带源地址、BIER模式维护三方面差异。S类、紧急开发类。
- **功能**：① 新增`src-dt4/src-dt6 locator X sid Y`（IPv4/IPv6地址族视图）及undo；以locator name/ipv6-sid为两级key的AVL树维护AF信息与下驱动成功标志，配置前查跨AF冲突。② 向 SRv6 侧申请占用/释放源地址 SID（新增 src-dt4/src-dt6 两类占用释放功能；占用失败则配置不生效但不删配置），收到 SRMS 侧 SID 新增通知后下驱动、置 available=yes；经 SRv6 侧注册响应消息（本地 SID 增加/删除、静态 SID 可用）触发重占。③ BGP 1/3类路由源地址由 MSI sub-sub TLV 改为 SRv6 Service Sub TLVs，新增子消息类型与对应属性结构（携带 SID、FuncType 及 locator 四段长度），双 TLV 按 sub-domain 封装模式选源。④ MVPN 的 BIER 配置节点（以 SD 为 key）新增 BIER 封装模式字段（BIERFIB 配置同步），模式变化清表项、撤路由、重发1/3类路由，复用underlay配置变化处理。⑤ 新增display multicast-bierv6-source configuration（暂定名），bier configuration加Mode。⑥ 备升主与SRv6平滑：平滑请求消息采用 SMOOTHSTART/DATA/END 三段标记法（带标记=locator 未在 SRv6 侧生成，等待 SID 重占完成）；ISSU（含DBM兼容）/IRF/进程亲和性，保持跨域。
- **难点**：跨MVPN/SRv6/BGP/BIERFIB/MBR/MFIB多组件配合，占位失败挂起等事件重占位、平滑需区分SID是否在SRv6侧存在、两模式源地址分开维护需兼顾；文档标注风险较高、周边影响与验收工作量较大。
- **代码量**：总5.1K LOC，11个功能点（BIER模式维护、BGP收发适配、源地址配置/维护/解除占用、与SRv6及MBR/MFIB平滑、显示命令），单点150~500 LOC、难度系数0.8，全部移植C代码；整体难度"较高"，难度系数1.2094，人力投入2人（开发付惠泉、林方钰，设计马晓忠）。

### 24. 19K-X 适配 Comware V9——BIER 驱动接口适配｜ CR19K-X/SR88 ｜ 5800 行（新开发）
- **背景**：原始需求源自 TM202109174294【SR88 B75】BIER IPv4/IPv6 移植到 V9 Trunk（需求编号 202501030007-89）。V7 中驱动初始化时向平台注册一组 BIER 驱动接口结构，内含 BIFT 变化、FRR 变化、表项（ENTRY）变化、隧道（Tunnel）变化四类函数指针，平台与驱动同进程、直接函数调用，一个 BIFT 表项拆成 BIFT/ENTRY/FRR 多个动作依次下驱动；且 V7 实现不满足 V9 进程间通信三要求（消息结构 ISSU 化、TLV 传递、多 TLV 打包），故 V9 转发/驱动进程分离后须整体重新设计。按驱动适配设计勾选：V9 采用 LIPC 长连接+共享内存通信、由转发进程下发驱动（组播数据参与转发、组播表项频繁大批量下驱动）。
- **功能**：转发进程侧定义统一的 BIER 变化处理驱动接口（入参为消息长度＋消息缓冲区），缓冲区为 TLV 打包消息：顶层为打包头（同一 PACK 内的子 TLV 视为完整消息、后级 TLV 继承前级 TLV 携带的驱动上下文），一级 TLV 分 BIFT 变化、FRR 变化、表项变化、隧道四类，对应 V7 的四个驱动接口；各一级 TLV 下挂二级"头/旧信息/新信息"TLV（隧道类再挂三级 bitstring 信息 TLV）；消息内上下文字段区分 global/local，VN 句柄由指针改为 UINT64 整型 VNID。平台侧对该接口做三态分发：优先 dlopen/dlsym 动态加载产品驱动库取实现；平台支持 LRPC 时走 LRPC 版本——经 LRPC 客户端远程调用（指定 BIER 模块 ID、函数号、驱动进程内的同名处理函数名、长连接、ULONG 返回类型、长度＋共享内存双参数类型）调用驱动进程内的同名处理函数，此时缓冲区必须直接用共享内存申请；否则走平台空桩。评审采纳方案一：平台提供 BIER LRPC 插件库（配置定制于 PUBLIC/init/all/facility.def 与 lrpc/plug.conf），在驱动进程解析 TLV 并还原 V7 接口形式、经 dlsym 取产品实现；下驱动失败重刷由驱动经资源重刷接口（覆盖 BIFT/表项/隧道/FRR 四类资源）通知平台，V9 改发 LIPC 消息复用 BIERFIB 本地端口、新增驱动消息类型，打包进新增的 BIERFIB 库；另有产品在转发进程直调的组播收包/收 OAM 报文两个接口保持 V7 内部实现。simware 驱动桩：在 simware 驱动源码目录下新增 BIER 驱动桩子目录（含命令行插件两个源文件、桩实现源文件编译为 simware 版 BIER 驱动库、resource 下 CLI ini/rc 脚本），驱动桩程序链接该库并在驱动初始化时完成注册，经用户态 CIOCTL 通道（DUMP 端口 0x1257、IOCTL 服务初始化）提供 debugging system internal driver-stub bier event、driver-stub bier set cmd-type cmd-value [slot]、display system internal driver-stub bier state 三类调试命令；MVPN SA 信息接入 V9 统一组播路由变化驱动接口，新增"组播服务 ID"驱动命令字并以 TLV 结构下发、驱动侧还原为设置组播服务 ID 的内部接口；NPS 单板私网 MLL 表 BIER 出接口评审采纳方案一（平台只发隧道 ID、驱动自存数据，经隧道信息查询接口取 SI/BSL/bitstring）；NG-MVPN P2MP 隧道 VN 适配评审采纳方案三（VN ID 与 VN 信息都下发），表项消息新增两个出接口子 TLV——mLDP 隧道子 TLV（携带三层 VN 结构与逐 ECMP 下一跳结构数组，新旧两条消息）与 RSVP-TE 子 TLV（携带 MPLS NHLFE 转发信息）。
- **难点**：V7 一对一函数调用改为 TLV 打包后，子 TLV 依赖前序 TLV 返回的驱动上下文，需以顶层打包结构封装保证包内继承，而不同 PACK 间相互独立不能一起下驱动、由平台控制；大批量下驱动性能依赖 LRPC 长连接+共享内存传递消息缓冲区；消息结构全部 ISSU 化；V7 的 VN 句柄是指针无法跨进程传递，经评审确定平台下发 VNID（方案一）；下驱动失败重刷同样评审采纳驱动控制的方案一，避免平台定时器反复重刷。
- **代码量**：总 5800 行（新开发 5800、移植 0），折算 5800 行，26 个功能点，全部 C 语言、无移植；难度系数 1.5557（1.55574252043269），整体难度/周边模块影响度/验收测试工作量/风险均为"中"，人力投入 3 人（开发：付惠泉、张磊、林方钰，设计负责人刘海丹）；无新加进程，支持 ISSU、vSys 天然支持、SR88 不支持 M-LAG；验收产品先 simware（SR88 产品进度较慢，2 月底后验收），用户态占比 10%、配置插件占比 5%。

### 25. 19K-X 适配 Comware V9——组播补齐（11 项需求：公平组播 组播 NAT NETCONF 补齐等）｜ CR19K-X/SR88 ｜ 20300 行（新开发4000 移植16300，折算6260）
- **背景**：19K-X（SR88）路由器适配 Comware V9 的组播补齐项目（需求 202501030007-113，移植类，B75→V9 Trunk），汇集 11 项需求：公平组播（移植，需求单【92504300009-1】中金所）、组播NAT（202503120022 平安科技）、首包超25出口（问题单 202206280695）、IGMP主机跟踪（201510230012）、组播流量速率统计（202104221913 无锡电信）、NSR状态查询（201703040013 电信MSE集采）、B75 MSDP SA过滤增强（201805050004）、命令行依赖acl支持commit（202407310085，含 TB202311280972 移植）、组播路由条目NETCONF（202106075471 电信集采Telemetry）、NETCONF补齐、平台启动优化DBM批量恢复（202209020575）。验收产品：SR88 与 simware。
- **功能**：①公平组播：系统视图 `multicast forwarding fair enable`（IPv6 为 ipv6 multicast forwarding fair enable），MRIB 注册命令行→MFIB→LRPC 下驱动，微码广播表带公平标记按 MID 逐包轮询出接口；不支持的板输出不支持公平组播的 syslog 提示，已有表项需 reset multicast forwarding-table 后生效；NETCONF Mroute/Ipv4(6)GlobalConfigs 增公平转发（FairForwarding）列，simware 只测命令行/下驱动。②组播NAT：接口视图 `multicast nat-alg pim { inbound | outbound }*`，仅 PIM SSM/IPv4/源地址一对一，需另配到 NAT 源的静态路由；协议面在内核组播软转发路径对 PIM JP 报文做 NAT ALG（复用 NAT 模块的接口判断与载荷地址转换），SR88 仅 inbound 软转，本项目新增 outbound；NETCONF Ipv4Interfaces 增 PIM NAT ALG 列。③首包超25出口：MFIB dummy 转正后缓存首包入检查链、1 秒定时器兜底，add oif 向新增出接口补发。④IGMP/MLD主机跟踪：host-tracking enable（全局/接口）、igmp/mld host-tracking limit（缺省 4096）、display igmp/mld host-tracking。⑤速率统计：display (ipv6) multicast flow-statistic source/group/interface/[s-vlan][c-vlan]{inbound|outbound}，下驱动 ACL 统计 2 秒、主控板经 LIPC 汇总各板，经平台驱动接口实时取统计值，simware 桩恒返 0。⑥NSR查询：display non-stop-routing status 汇总命令，以新增 NSR 动态库＋命令行插件实现，各模块经 NSR 引用注册与事件初始化接口注册回调，PIM/IGMP/MLD/MSDP 接入。⑦MSDP SA过滤：peer sa-policy 新增 import-source，对本地始发 SA 按 peer 过滤，优先于全局 import-source。⑧ACL commit：延迟生效视图 undo 被引用 acl 时报错提示，经 ACL 事件注册接口关注"ACL 组删除检查""特性名登记"两类事件，引用存 COMMIT 全局 url（如 /PIM/AclUsed/SortByThreeKey）。⑨NETCONF 移植 Mroute/Ipv4Statistics、Ipv6Statistics（组播路由条目计数字段遍历 VRF 累加）。⑩NETCONF 补齐 MLD/Groups、MLD/SrcList、Mroute/Ipv4(6)RpfStaticRoutes，支持 XML2CLI。⑪DBM 批量恢复：经平台接口批量通知接口 active/板插入事件，PIM 主流程配合 DBM 批量查询接口（登记 key、批量判断 key 存在、批量取数据）批量查未生效 DB。
- **难点**：①B75→V9 移植差异大：锁/内存/epoll/LIPC 等公共修改，下驱动改 LRPC 调驱动进程+TLV 转换，simware 需驱动桩；IGMP 需避开 B75 BRAS 代码冲突。②组播NAT 最复杂：JP 载荷 ALG、快转表（转换前SG）与慢转表（转换后S）不一致、跨 vpn 硬转不支持，仅软转且不支持 VPP。③速率统计分布式：多板序列号匹配汇总，防 ACL 残留/并发/前台死等。④mrib 每次最多下发 25 出接口且与 pim 并发有抵消机制、无法标记结束，MFIB 需缓存补发。⑤需求1/2/5 纯硬件，本期 SR88 暂不支持，只能 simware 测命令行/下驱动；配合组：NSR 查询需 BGP/OSPF/ISIS 路由侧各 0.2K、NAT 0.2K、DBM 配合 OSPF 路由管理 0.1K。
- **代码量**：总 20300 行（新开发 4000，移植 16300），折算 6260 行；明细（LOC）：公平组播 1700、组播NAT 2100、首包25出口 400、主机跟踪 5000、速率统计 2000、NSR查询 1400、MSDP SA 300、ACL commit 4800、路由条目NETCONF 500、NETCONF补齐 1700、DBM 400。整体难度与复杂度：中，周边模块影响度：较低，人力投入：3，验收测试工作量：较大，风险：中，难度系数 1.1098；用户态占比 90%、配置插件 10%、普通难度命令行 20%；SOW 撰写 40 人时。

### 26. 园区交换机 B70 特性同步到 V9 trunk——MCAST 二期｜ V9 trunk ｜ 25350 行（移植18350 新开发7000）
- **背景**：为补齐 V9 trunk 与园区交换机 B70（V7）分支的特性差距，将 B70D064SP/B75 等分支上已落地的组播特性同步移植到 V9 trunk（MCAST 二期，需求编号 202211150075）。项目类型为"新V模型-S类"，附加属性为"移植类"。7 项特性覆盖 2018-5.7k 跨VPN组播、2021-13.9k 组播可维护性、2019-0.3k PIM支持地址借用、2020-0.9k 组播快速切换RP（腾讯需求）、2021-1.4k 默认过滤 239.255.255.250（SSDP）组、2022-0.7k 命令行控制出接口限制（轨交方案），以及问题单 mvpn mdt 支持私网 IPv6（0.1k）。部分特性代码已随版本同步到 V9 但从未调试（如跨VPN命令行），本次需逐点调试与适配。验收产品为 simware 与 V9 园区接入交换机。
- **功能**（7 项特性，57 个功能点）：① 跨VPN组播：MRIB 视图 `multicast extranet select-rpf [l3-vni vxlan-id | vpn-instance vpn-instance-name] {group/mask | source/mask}` 及 IPv6 MRIB 视图对应命令配置跨VPN RPF 选路策略（l3-vni 字段支持非对称跨VPN，一个 VPN 内可配多条，含冲突检查/保存/最优匹配及源/接收者 VPN 删除事件处理）；`display pim/ipv6 pim routing-table` 增加 `extranet {source-vpn-instance | source-public-instance | receive-vpn-instance | receive-public-instance}` 显示；NETCONF 补齐 Mroute/Ipv4ExtranetSelectRpfs、Ipv6ExtranetSelectRpfs 整表；扩展 VPN 引入引出逻辑虚拟接口（PIM 中 NBMA 口已占用 MID，需支持一个进程内按多个不同 MID 创建虚拟接口，公共接口改动需回归 NBMA/MUA/MUPE 口的创建/删除/倒换平滑）；入 VPN downstream、出 VPN upstream 状态机对 SM/SSM/BIDIR-SM 三种模式特殊处理（源 VPN 路由变化、接收者 VPN 内加入特殊处理）；内核 MFIB 维护跨 VPN 转发表项并扩展板间同步消息与软转；平台已对跨 VPN 表项做合并，故无需驱动修改。② 组播可维护性：移植 B75 MRIB event-log（原项目 5.9k，MFIB event-log 已在 VPP 项目移植），MRIB MBR event-log 的 display/reset/size 命令族 IPv4+IPv6 双栈，由 Probe 视图改到任意视图；约 200 种二级类型 log 内容 lib 化（框架+PIM/IGMP 相关 log 及适配，PIM/IGMP 代码移入 lib 并重命名），MBR 读写 log、约 15 类流程埋点（每类 20/40 处）。③ MVPN event-log（原项目 6.8k）：`display/reset system internal multicast-vpn event-log {route | ha | bgp}`，覆盖 HA 平滑、与 BGP 收发的 1/3/4/5/7 类路由及其他消息、BGP MDT、与 MBR 收发、RSVP-TE、mLDP 通信及内部处理 log。④ PIM/MRIB/IGMP/MSDP/组播 VXLAN 支持地址借用（5 个功能点）。⑤ PIM/PIM6 视图新增 `c-bsr holdtime`/`c-bsr interval`（配置 C-BSR 等待自举报文超时时间与发送间隔），响应快速切换 RP。⑥ 系统视图 `multicast permit ssdp-group` 默认过滤 SSDP 组 239.255.255.250，MRIB MBR/IGMP/PIM/MFIB 四处策略变化与策略检查联动，含产品定制参数解析。⑦ 系统视图 `multicast cpu-forwarding max-copy-count count` 控制软转复制上限，含 NSR、配置平滑、MFIB event log 与 netconf（含 IPv6）。另 MVPN 视图放开 `address-family ipv6`，调试 MDT 私网 IPv6（Data-Group/Default-Group 显示命令），V9 已继承代码、仅需调试。公共需求支持 ISSU、IRF。
- **难点**：跨VPN组播为最大难点——SM/SSM/BIDIR-SM 三种模式的状态机均需入/出 VPN 双向特殊处理；虚拟接口为公共接口，扩展需回归验证 NBMA/MUA/MUPE 原有功能；软转适配难度系数最高（1.6），MFIB 处理（1.4）、板间同步/下驱动/表项组织（各 1.5）均为分布式多线程代码，需板间数据备份；MRIB log 涉及约 200 种二级类型的 lib 化重构与 15 类流程埋点，SSDP 的 MFIB 策略检查难度系数 1.88 为单点最高；B75 分起新增命令行要求支持 commit；验证涉及复杂组网，数据转发类高效代码占比高（用户态约 70%，配置插件占 15%）。
- **代码量**：总 25350 行（移植 18350 新开发 7000），57 个功能点，难度系数约 1.34（1.34223438461538），人力投入 3 人（开发袁廷坤 17269、金鑫 24961、林方钰 YS3904，设计负责人/跟踪协调人刘海丹 09104）；整体难度与复杂度较高，周边模块影响度中，验收测试工作量较大，风险中。


## 五、V9 trunk 特性补齐与可维护性

### 27. B70 MULTICAST trap 补齐｜ V9 trunk ｜ 7800 行（移植6800 新开发1000）
- **背景**：V9 TRUNK 上【V7/V9 特性补齐】【园区交换机】需求，现有模块增强、附加属性移植类。V9 中仅 PIM 实现了少数几类 trap（实现集中在 PIM 主进程的 trap 发送、标准 MIB 通知与 MIB 树管理等少数代码文件中），IGMP/MLD 及 Snooping、MCS 等模块的 trap 开关与告警输出缺失，无法满足运营商以 TRAP 为实现方式的故障告警/故障恢复运维需求。项目源自二期 PIM trap 节点补齐需求（B70D064sp，河南移动，maxiaozhong 12899）的推广补齐，告警内容以《Comware MIB 参考》11-组播分册为准。纯用户态实现（100%）、不涉内核态/新加进程/驱动/外部组件，HA 模型 M3，MLAG/vSys 天然支持，新增 CLI 需支持 commit 并适配 XML2CLI。
- **功能**：① 系统视图新增 `snmp-agent trap enable igmp [ join | leave ] *`、`igmp-snooping [ entry-refresh-failed ]`、`mld [ join | leave ] *`、`mld-snooping [ entry-refresh-failed ]`（均含 undo，支持 commit）；`snmp-agent trap enable pim/pim6 [ ... ]` 各新增 elected-bsr-lost-election、interface-election、invalid-join-prune、invalid-register、private-interface-election、private-neighbor-loss、private-new-neighbor、private-rp-mapping-change、rp-mapping-change 共 9 个节点（candidate-bsr-win-election、neighbor-loss 已有）。② 告警节点/OID：私有 PIM 的 hh3cPimNeighborAdd、hh3cPimNeighborLoss、hh3cPimRPMappingChange、hh3cPimInterfaceElection；公有 PIM 的 pimInvalidRegister、pimInvalidJoinPrune、pimRPMappingChange、pimInterfaceElection；私有 MCS 的 hh3cMcsEntryRefreshFailAlarm；私有 IGMP/MLD 的 hh3cMgmdStdGmpJoin、hh3cMgmdStdGmpLeave；全局节点 pimRPMappingNotificationPeriod（仅此节点涉及 MIB 测试）。③ 框架推广：以 PIM trap 框架为样板向各模块推广——公共部分覆盖 trap 框架的注册/去注册/与 SNMP 通道重连、事件处理与告警通知回调、重连超时回调、trap 开关键值与标志位的读写及字符串互转等，以及限速组件（限速预留，评审明确暂未真正用到、可先不做）；单条告警流程：告警触发点产生 trap → 判断对应 trap 开关是否使能 → 调 SNMP 接口发送，MIB tree 告警节点（如邻居丢失告警节点）按 MIB 文件生成；命令行链路为命令行插件解析/安装 trap 开关命令、生成配置下发、配置进程设置 trap 开关并生效的完整链路。各模块差异按估算模型裁剪：trap 公共部分每模块约 300 行，每条消息约 150 行（每增一参数 +30 行），trap 命令行每模块约 300 行（每增一消息 +15 行）。
- **难点**：整体难度与复杂度较低、周边模块影响度低、验收测试工作量与风险一般；主模块 Pim、igmp、mcs 修改，无功能和性能影响，无配合/相关模块。新增代码涉及多线程、分布式、板间数据备份（均 YES）；配置插件占比 25%，MIB/命令行普通难度代码占比 25%。拆 21 个功能点（难度系数 0.5~1.0），大项含：IGMP 后台 trap 框架 500（新开发）、MIB tree/notify 各 400、加入/离开告警各 350；hh3cpimstd MIB 框架移植 800、mcs trap 框架移植 800、pim snmp 框架整改 300；PIM neighbor-loss/rp-mapping-change/interface-election/new-neighbor 告警各 300、pimInvalidRegister/pimInvalidJoinPrune（无效注册/无效加入剪枝）各 200；MCS 驱动失败告警 400、命令行 400、流程输出告警 200（新开发）。2 名开发（linfangyu/ys3904 负 IGMP 与部分 PIM 告警，fuhuiquan/22570 负 PIM 告警与 MCS），设计/评审 yuantingkun。测试覆盖 trap 告警与命令行功能、与其他模块 trap 开关及 IPv4/IPv6 组合、trap 输出性能、异常状态输出。
- **代码量**：总 7800 行（移植 6800 新开发 1000）；难度系数 0.78（明细计算值 0.782451923…）；人力投入 2 人；整体难度较低、周边影响度低、验收测试工作量一般、风险一般。交付件含 SOW、MIB 文件/参考/说明书、系统测试用例与报告、命令参考、配置指导、特性说明书、YANG 文件、静态检查报告等。

### 28. YANG 约束检查-组播（EANTC 测试复盘）｜ V9 trunk S12500 ｜ 2700 行（新开发）
- **背景**：EANTC 测试中思科 NSO 控制器下发配置时前台不报错、到设备才失败，类似命令行真正 commit 时才报错。前台检查原由命令行插件 C 代码实现，YANG 检查则可直接在 YANG 文件中用语法实现、无需改 C 代码。项目源于 EANTC 复盘，已经系统部汇报和 PL-CCB 裁决，2024Q3 交付；范围仅 xx-config.yang；目标"能用 YANG 检查的都得用 YANG 检查，不能用的说明原因"。
- **功能**：对 IGMP/MLD/Mroute/PIM/MVPN/MSDP/IGMP/MLD/PIM Snooping 九模块逐表补齐：default、units、description；leafref 引用（VRF→l3vpn、IfIndex→ifmgr、VLANID→vlan、VsiName→L2VPN、MSDP Peers VRF→MSDP Instances），实现存在性校验与被引用禁删；pattern 实现 acl number/name 联合体（各列范围不等、name 非 all）和十余类地址合法性（单播按含否 0 地址、组播按含否保留地址/管理域/FF 保留前缀分类）；must 列间关系（如 MSDP KeepaliveInterval<Holdtime）；when 依赖（IgmpVersion 依赖 Acl、snooping 全局 Enabled 开关）；choice case 互斥（PIM JoinPolicy）；max-elements 实例规格（Mroute Instance 按 l3vpn 定制 1024）；count() 实现 VxlanMGrpMem（VXLAN 组播成员表）仅一接口使能（count(...)=1）；及产品 deviation（NsrEnable 等）。
- **难点**：逐表梳理 NSO 实测失败场景归因（ExtranetSelectRpf 多类 must 冲突、非行创建表加 must 删最后一条误报、PIM sm/dm 互斥需经接口索引反查 VPN、MSDP 密码列不回前台不能加 must）。确认"无法用 YANG"清单：依赖后台/驱动运行数据、IP+掩码运算、复杂冲突如 pim sm/dm、联动创建（mvpn MTI 口）、联动删除（组播边界覆盖）、依赖操作类型。NSO 无法输入空串、空格下发被转空串致审计报错，推动 L3VPN 用 "__public__" 表示公网、由框架双向转换。
- **代码量**：总 2700 行（全部新开发），纯用户态 C、非移植；难度系数 0.9（整体 0.954），难度/影响/风险中，验收测试工作量较大。分模块（含约束检查与描述基础修改）：IGMP/MLD 各 300、Mroute/PIM 各 400、MVPN/IGMP/MLD Snooping 各 300、MSDP/PIMSnooping 各 100、公共地址检查 200；人力 2 人，SOW 撰写 60 人时，验收产品 S12500 与 simware。

### 29. 全特性单机 ISSU——二层组播（igmp/mld/pim snooping）｜ 园区接入 S9820/S6550x ｜ 1800 行（新开发）
- **背景**：园区接入交换机 S9820-8M 等产品（SAP/验收产品 S9820/S6550x）要求二层组播支持 V9 单机 ISSU 软重启、升级期间流量不中断（需求编号 202405170043-1）。V9 单机 ISSU 原理：旧主容器（slot1）响应 ISSU 软重启事件、驱动将软件表项保存到 ISSU 文件，备容器（slot1.1）启动后先从 ISSU 文件恢复表项、再走原有 HA 流程从旧主拉数据，备升主后删除旧主容器；自始至终硬件表项只有一份且升级中不修改，故流量不中断。支持范围 igmp-snooping / mld-snooping / pim-snooping / ipv6 pim-snooping（IPv4/IPv6 双栈需适配），明确不包括 mvlan（后续需求跟踪）。总体设计思路：能通过现有数据恢复的不经 ISSU 文件恢复（省内存、免专用代码、利于扩展）；始终保持 MFIB 表项与驱动一致，仅做平台内部平滑、不做平台与驱动的平滑。HA 模型 M2，运行于 CEN/MPU，不涉及内核态、不新加进程，MLAG/vSys 天然支持。
- **功能**：涉及 MCS/MFIB/MSIB 及转发驱动等模块，共 12 个功能点。①旧主容器响应软重启：MFIB 响应 ISSU 软重启事件记录全局变量，转发侧判断正在 ISSU 软重启（原子操作、不用数据锁）后不再处理 MCS 消息、不下驱动、不通知 MFIB，直接返回成功以防 MCS 反复失败重刷，转发侧缓存的 MCS 消息同步受控；MFIB 收转发侧 MCS 消息的队列需继续处理（队列内均为二层已下驱动表项，保证二层表项与驱动一致）；MCS 响应同一事件记录全局变量、不再下驱动，且不能禁止同步备容器。②备容器启动（核心新增）：备板走板插入流程时本板 MFIB 无数据，仅从旧主 MCS 拉数据无法区分表项是否下过驱动，故新增"备 MCS 从主 MFIB 拉数据"流程（复用从本板 MFIB 拉数据的流程），拉到的数据创建带 kernel 标记的 MSIB 表项（对应 display l2-multicast ip，不创建 group 表项，对应 display igmp-snooping group），下发给转发侧、由其通知 MFIB 创建转发表但不下驱动；拉取开始前发消息通知转发侧禁止下驱动、结束后通知恢复。③再从旧主 MCS 拉数据完成升级前后表项区分：升级前已建表项收到平滑消息后创建 group 表项并清除 kernel 标记、不再下发转发侧；升级期间新建表项创建 group+MSIB 表项、下发转发侧并下驱动、通知 MFIB 创建转发表；升级期间删除的表项收不到平滑消息、kernel 标记残留，平滑结束后检查 MSIB 表项，有 kernel 标记则删除表项、下发转发侧下驱动删除并通知 MFIB 删除转发表。④备 MCS 从主 MCS 收完数据并下发完成后才设置进程 running，倒换后 MFIB 未处理完的消息继续处理；备升主流程无需特殊处理。⑤ISSU cancel：本项目暂无需支持，但设计上 MFIB 与 MCS 响应 ISSU 取消事件重置全局变量，MCS 删除所有运行数据走进程重启流程（先从 MFIB 拉数据、再从 DBM 恢复配置、与周边模块平滑、收报文恢复 group 表项），期间新增表项丢失需报文重新学习、删除的表项需等老化恢复。⑥配套：MCS 向 MFIB 发送"是否下驱动"消息、MCS/MFIB 双侧 HA 流程修改及可维护性 log；升级前后 MCS 维护的 drvcontext 需保持一致（从原主 MFIB 拉取获取 drvcontext），已下驱动表项不得重复下发。
- **难点**：①升级期间备容器须恢复出完整数据并区分"已下驱动/未下驱动/已删除"三类表项，靠 kernel 标记状态机贯穿 MSIB 表项生命周期（拉取时打标记→平滑时清除→残留即删除）；②转发侧下驱动控制方案经评审决策：方案一（现有消息加标志位）因消息类型多、增加困难且后续易遗漏被否决，采用方案二——MCS 与转发侧 MCS 的通信通道新增专用消息类型（拉数据前禁止下驱动、拉完恢复），评审结论为方案二；③升级全程不修改硬件表项、保证 drvcontext 一致性，已下驱动表项不能重复下驱动；④分布式多板架构下 MCS 绑定系统主、MFIB 数据仅来源于 MCS，未做 MCS 与 MFIB 双向平滑，主备倒换时序（进程 running 设置时机、MFIB 未处理完消息续处理）需严谨设计。
- **代码量**：总 1800 行（全新开发，非移植，C 语言），12 个功能点，MCS 侧 6 项 1050 行（注册处理 ISSU 事件 250、从旧主 MFIB 拉数据 300、旧主拦截下行消息 50、HA 流程修改 50、向 MFIB 发送是否下驱动消息 100、可维护性 log 300），MFIB 侧 6 项 750 行（注册处理 ISSU 事件 250、旧主拦截 MCS 下行消息 50、旧主处理备 MCS 拉数据请求 100、HA 流程修改 50、处理 MCS 是否下驱动消息 100、可维护性 log 200）；整体难度系数 1.61，整体难度与复杂度较高、周边模块影响度中、验收测试工作量较大、风险中；人力投入 3 人（开发 liuhaidan/ouyangning/linfangyu，设计负责人 liuhaidan）。

### 30. 全特性单机 ISSU——三层组播｜ 园区接入 S9820-8M ｜ 1800 行（新开发）
- **背景**：V9 单机 ISSU 升级时旧主容器（slot1）响应软重启事件、备容器（slot1.1）从 ISSU 文件恢复后再走原有 HA 流程从旧主拉数据、后备升主，硬件表项自始至终同一份且升级期间不修改，故流量不中断。本需求（202405170043-19，现有模块增强，新 V 模型-B 类）要求在园区接入 S9820-8M 上让三层组播框架支持单机 ISSU 软重启、转发不中断，覆盖所有三层组播特性、组播 VLAN、二层组播 VXLAN（ISSU 一期未含，本项目补齐）。项目启动时框架尚不支持 M-LAG 的 ISSU 且依赖的二层组播 ISSU 亦不支持，故暂不支持 M-LAG。涉及 MCS、MFIB、PIM、IGMP 四个主模块修改，用户态 C 实现、不加进程不加线程。
- **功能**：旧主 MFIB 响应 presoftreboot 置全局变量，不处理 PIM 消息、不板间同步（选 MFIB 拦截是因 MFIB 集中控制，可丢弃 PIM 下行消息队列，二三层同使能时可控制不扩展三层表项）；组播 VLAN 与三层组播共用 MFIB 的 presoftreboot 全局变量，之后不再处理 MVLAN 消息。备 MFIB 走原板插入 sync 流程从旧主 MFIB 拉数据（含出接口及出端口，NG-MVPN 的 P2MP VN 在 sync pull 前已恢复），拉完才回复就绪消息；因 MFIB 原未注册 fnotify，新增 fnotify 注册（参考 V7 的对应实现），防止备容器过早 ready、数据不全即主备倒换。dummy 表项、大规格下驱动失败（复制资源绑定失败）表项改为同步备板维护软件信息、不下驱动，避免备升主后驱动残留。IGMP/MLD/PIM/IPv6 PIM 默认 NSR（V9 单播路由已默认、组播此前未支持），新增命令 `igmp non-stop-routing`、`mld non-stop-routing`、`pim non-stop-routing`、`ipv6 pim non-stop-routing`，缺省情况与设备型号有关；PIM 备从 PIM 主拉数据不下 MFIB，PIM 初始化后不设 running，等批备结束通知后再置 running。升级期间表项增删由 PIM 主通知 PIM 备维护，MFIB 忽略 PIM 消息不同步备板；备升主后新主 PIM 从新主 MFIB 拉数据、与周边模块平滑结束再与 MFIB 平滑，变化表项此时才下驱动（ISSU 时长约 4 分钟）；未使能 NSR 时 ISSU 期间删除的表项无法判断、仅等老化。二三层混跑时 presoftreboot 后二层组播表项变化不再扩展到三层表项。MTI 编号申请走 V9 配置下发通道、不下驱动故不拦；ISSU cancel 暂无需支持。共 14 个功能点：旧主/备 MFIB 拦截 PIM 下行消息与下驱动、MFIB 支持 fnotify 注册、同步 dummy 表项、同步绑定复制资源失败表项、PIM 响应 ISSU 事件、PIM/IGMP 默认 NSR、组播特性适配、可维护性 log、二层组播 VXLAN 适配、组播 VLAN 三项（拦截 MVLAN 下行消息/下驱动、MVLAN 流程适配）。
- **难点**："能通过现有数据恢复的不走 ISSU 文件恢复"——存 ISSU 文件耗额外内存且需专用代码、不利扩展，全部经主备同步原有流程恢复，不新增内存；MFIB 表项与 drvcontext 全程与驱动一致、已下发表项不重复下驱动，仅做平台内部平滑、无需平台与驱动的平滑；平台与驱动响应 presoftreboot 有先后，须保证平台先响应（已确认框架可保证），否则删除类命令不关心返回结果、平台无法重刷；拉数据与倒换时序保证（MFIB 拉完数据才回复就绪、PIM 批备结束才置 running）。
- **代码量**：总 1800 行（全新开发 C 代码，非移植），14 个功能点，难度系数 1.61（精确 1.60922596153846），整体难度高、周边模块影响度中、验收测试工作量大、风险中，人力 2 人（开发林方钰、设计刘海丹）；主要拆分：fnotify 注册支持、PIM 响应 ISSU 事件、PIM/IGMP 默认 NSR、VXLAN 适配、MVLAN 流程适配各约 200 行，dummy/绑定复制资源失败表项同步/特性适配/可维护性 log 各约 100 行，四个拦截点各约 50 行；用户态占比 50%，验收产品 S9820/S6550x。

---

## 附注
- `项目SOW/` 下共 31 份任务书，本文档 30 个条目一一对应。
- 所有命令、规格数字、LOC 数字均引自任务书原文；"折算"行为任务书官方折算值。正文中的函数/结构体/消息等代码标识符已按要求删除，改以中文描述表述（仅保留命令行、NETCONF/YANG 显示项与 hh3c 开头的 trap/MIB 名）。
- 每条含背景 功能（命令行、显示项、规格数字、故障场景、评审决策） 难点 代码量（LOC、功能点数、难度系数、人力、用户态/配置插件占比等官方量化数据）。